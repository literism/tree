我发现现在的整个过程有很大的逻辑漏洞。我需要你仿照现在的项目代码，在新文件夹下写一个全新的项目。项目组成如下：
1. 参数管理。使用与现在项目完全相同的管理方式即可。
2. 数据划分。只需要把topic分为train和test，不需要test_easy和test_hard，其余的把现在的data_split.py照搬过来即可。
3. 生成总结。为目标结构树的每个节点都生成一个总结，用modeling/deepseek_api.py中的run_prompts_to_texts方法，直接调用api来实现。在prompt中，输入当前节点的路径和内容，让大模型写一个summary。这个summary必须包含两部分，1. 这一类讲了什么。2. 这一类包含哪些方面。
现在的wikipedia_structures_final.json的每个节点都有一个content字段，保存了这个节点下的段落内容，用这个作为输入prompt的内容。如果这个节点下有子节点，那么用前三个子节点的title + 段落内容拼起来作为段落内容输入。
加一个测试模式，只试运行一个topic，一次生成一个结果，让我看看api的返回结果是否符合我的要求。
4. 分类系统。你需要设计一个prompt，由大模型来进行分类。输入是一篇文章、当前要关注的主题（包括topic，路径上节点的summary）、当前一些类别的summary，让大模型根据各个类别的描述，判断并输出文章属于哪些类别（类别编号）、还有没有与主题有关但不在这些类别中的内容（true or false）。你要注意：
（1）用严格的格式约束模型输出，只输出类别编号。
（2）在判断是否需要新类时，必须强调，是存在与主题有关但不在这些类别中的内容时，才输出true。比如一个文章属于A - a1、B - b1，当前主题是A，当前类别中有a1，那么就不能输出true，因为b1与A无关。
（3）现在是用路径作为当前节点的表示，而现在不一样，需要给大模型topic和当前节点的summary，作为主题，让模型知道当前位置。与title和subtitle无关了。后续也是一样。
其实和现在的项目很类似，只是不再用一个标题来表示一个类，而是用一个summary来表示。
5. 生成系统。你需要设计一个prompt，由大模型根据现有的类和文章来生成新类。输入是一篇文章、当前要关注的主题、当前一些类别的summary，让模型根据文章内容，输出一些新类的summary。你要注意：
（1）用严格的格式约束模型输出，可以输出多个新类，每个新类生成一summary。
（2）必须强调，生成的新类和当前一些类别的summary必须有足够的区分度。生成的多个新类之间也必须有足够的区分度。也就是生成的summary必须边界清晰。一个summary包含两部分，要求与第三点一样。
（3）必须强调，生成的新类也必须在父类的summary之下。按照上面的例子，文章被分类到A以后，如果生成新类a2，那么A的summary范围必须覆盖a2，不能生成与B有关的内容。
6. 构建系统。与现在的构建系统类似。对每个topic都生成一个结构树，先把文章、主题和现有类别的summary给分类系统，让分类系统做分类，将文章分到子类中。如果分类系统判断有新内容，那么启动生成系统，生成一个或者多个新类，将文章加入新类。
7. 构造训练数据。训练数据分两部分，分类系统的训练数据和生成系统的训练数据。
对于分类系统的训练数据，分为四类数据。前三类与现在的组成类似。
（1）第一类是判断需要有新类输出。构造方式是，先和现在一样，对一个文章构造分类流程，然后对每一次分类，从所有的选项中删掉一些包含正确分类结果的类，作为输入，输出是剩下的正确分类结果的编号和true。
（2）第二类是判断不需要有新类输出。构造方法是，对于每一次分类，将所有的选项作为输入，输出正确分类结果的编号和false。
（3）第三类数据是叶子节点数据。构造方法是，对一个路径的末尾，输入的选项为空，输出的分类结果为空，判断为false。
（4）第四类数据是新加的。对于每次分类，输入的选项是空，输出的分类结果为空，判断为true。这部分数据模拟一个新类生成还没有子类时的分类需求。
这四类数据根据比例和总数进行采样得到最终数据。总数和比例放在config之中。
对于生成系统的训练数据，与分类系统的数据深度绑定。在构造分类系统的第一、四类数据时，将一样的分类结果、当前节点summary、文章作为输入，输出是新类的summary。只输出一个新类和输出多个新类的数据分开，根据总数和比例采样得到最终数据。
8. 训练模型。需要训练分类系统和生产系统。训练的代码请完全照搬现在的train.py。
9. 推理。直接调用构建系统，对test topic构建结构树。
10. 评估。可以完全照搬现在的evaluate.py，读入推理的结构树和真实的结构树，把叶子节点作为聚类的簇，计算ONMI。

整个是一个比较复杂的过程。有什么问题就直接问我。

回答你的问题：
1.1 对于所有的节点都要包含这两部分。实际上，第一部分是对这个类进行的解释，第二部分是对这个类进行明确的范围界定。
1.2 我现在想想，只选用前三个有些不合适。现在改成这样的逻辑：如果所有子节点的段落加起来超过了长度，那么就平均分配每个子节点应有的长度，然后对每个子节点段落限制长度。将所有的子节点标题和限制长度的段落拼接，作为这个节点的段落。
1.3 一次生成一个节点的summary，我需要保证api返回summary的质量，又不想浪费api额度。

2.1 首先T代表topic，它是根节点，是没有summary的。整个路径中，只需要给出上一个点的summary即可。比如 T - A - a1 - aa1，那么在进入a1进行分类时，只需要给出a1的summary即可。
2.2 是的，是指当前节点的子节点的summary列表。比如现在进入了A节点，那么需要给出a1及其兄弟结点的summary列表。

3.1 都可能被删，但在这里加一个限制吧，不要一次删除两个以上的分类结果。最多删除两个。如果正确分类结果只有两个，又都被删了，那么构建的输出就是分类结果为空，判断为true。
3.2 是的，这一类数据模拟的是 A 刚被创建出来，进入T - A后，A下没有任何子节点的情况。
3.3 先按照这个比例来写程序吧。

4.1 先暂定这个比例吧。
4.2 限制最多生成两个。如果一次生成多于两个新类，那么直接抛弃这个数据。这个作为一个参数，加到config中。

5.1 好名字，就用这个吧。
5.2 直接用现有的就行。

1. 需要加到config之中。这个数不需要太大，2048或许就不错了。
2. 是的。比如路径 T - A - a1，那么到了a1就是叶子结点了。
3. 是的。当然了我这里说的输出是简化输出版本。你需要严格的约束输出格式。
4. content是纯文本段落。不包含title，只包含段落内容。
5. 按照现在的划分逻辑即可。从每个topic类别中选一个作为test，现在的选择逻辑是选择文章数最接近250的。
6. 是的，多个新类就都要加入作为子节点。文章也都要加入到这些类中。


有两点忘记说了。
1. 在推理时，需要同时使用两个模型。我有两个显卡，各48g显存，我觉得最快推理的方法是，用vllm将两个模型分别加载到两个显卡上，一个模型占用两个卡的一半显存，另一个模型占另一半，然后推理。
2. 在推理时，要和之前那个项目一样，保证不同topic间并行，同一个topic内的每次推理都串行。因为现在的推理结果会影响后续分类。


我发现了一个问题。在项目的第三步、生成总结时，生成的结果又两个问题：
1. 生成内容的层次性不明显。
2. 生成了不需要的父节点的内容
比如一个路径是 T - A - a1，T是一个电影，A是电影的制作，a1是演员，a2是剧本，a3是音乐。那么生成A的总结应该是：
解释：讲了电影的制作要素。
范围：包含演员、剧本、音乐。
但是实际上，现在生成的总结类似于：
解释：讲了这部电影（这部分不需要）。讲了电影的制作要素。
范围：...
生成a1的总结应该是：
解释：讲了制作要素中的演员部分
范围：包含演员的信息、选角...
而不是：
解释：讲了这部电影、讲了制作要素（这部分不需要）、讲了票房（段落可能涉及了票房内容，但票房不属于A制作，重点关注的是A下属的a1）...
范围：包含票房、制作...
也就是说，目前，大模型生成的总结没有严格的包含在其父节点的总结之下，并且生成了一些不需要的信息。生成的内容应该层次分明。
修改prompt，解决这一问题。

我需要在构建两个训练数据集时，更精细的把控比例。
对于生成系统的数据集和分类系统的数据集的第一、二、四类数据，再将数据分为第一层、第二层及以后两部分，在哪一层构建的数据就归于哪一层，然后按比例采样这两层数据。

我还是详细的说一下构建过程吧。
1. 对每个文章构建分类路径。
2. 在每个分类点，按照分类数据第一、四类数据的比例，作为本数据集两类数据的比例。第一类中，再以generator_single_ratio作为概率，选择生成单个还是多个新类的数据。
3. 生成单个新类，则随机删除一些不是输出的当前summary，再随机删掉一个输出summary，然后将剩下的作为输入，删掉的那一个输出作为输出。
4. 生成多个新类，则随机删除一些不是当前输出的summary，再随机删掉两个输出summary，然后将剩下的作为输入，删掉的那两个输出作为输出。
5. 生成第二类数据，则输入的当前summary为空，输出就是输出的summary，最多两个。


刚才发现的这个错误非常严重。以防万一，我以例子的形式说明数据的生成过程，请你完整的检查代码。
现在有一个结构树：
T
  A
    a1
    a2
    a3
  B
    b1
    b2
  C
  D
有一篇文章，路径有[T - A - a1, T - A - a2, T - B - b1, T - C]
那么得到的分类流程是：T分类到A,B,C；A分类到a1,a2；B分类到b1；

对于分类系统
第一类数据
  第一次分类，可以得到一个数据，随机删掉一或两个正确结果，比如删掉A。那么，输入是T的summary(T是topic，所以summary为空),BCD的summary作为候选集，输出是BC和True。
  第二次分类，可以得到一条数据，比如输入是A的summary，a1、a3的summary作为候选，输出是a1和True。
  第三次分类，可以得到一条数据，输入是B的summary，b2的summary作为候选，输出是None和True。
  注意，除非只有一个候选结果且为正确结果，否则请不要把候选删空。如果所有的候选都是正确输出，那就选只一个删。
第二类数据
  第一次分类，输入是T的summary，ABCD作为候选，输出ABC和False。其他分类类似。
第三类数据
  每个路径都能生成。比如输入C的summary，候选为空，输出None和False。
第四类数据
  每次分类都能生成。第一次分类，输入T，后续为空，输出None和True。再比如第二次分类，输入A，候选为空，输出None和True。

生成系统
对应分类数据第一类，与第一类数据生成的方法完全相同，输入也完全一致，只是输出的是summary。
以第二次分类为例，输入是A的summary，a1、a3的summary作为现有的summary列表，输出是a1的summary。

对应分类数据第四类。构建方法和输入也完全相同。
例如第一次分类，输入T，现有的summary列表为空，输出AB的summary（不输出C，因为最多输出两个）

请仔细的检查数据生成过程，这是非常重要的事情。


我发现生成系统写的summary还是几乎没有体现出父子节点的继承关系，子节点的summary经常超出父节点的范围。
因此我重新设计了生成系统的输出和构建数据集的方式。
首先，重写生成系统的prompt，重新组织逻辑。
输入还是四部分：topic，父节点的summary，文章内容，兄弟结点的summary。
prompt中强调以下事情：
  只关注和topic有关的内容。
  生成的内容必须与兄弟节点有区分度。
  生成的内容必须在父节点的范围内。
  只生成一类。
  步骤是：
    在文章中找到和兄弟节点相关的内容。
    在文章中找到与父节点一致但不属于兄弟节点的内容。
    根据这一部分内容生成总结。
输出是如下格式：
{
  "brother_overlap": {
    "span": "文章中和兄弟节点相关的内容",
    "brother_node": [...], # 兄弟节点编号列表
    "reason": "比如：匹配兄弟节点中的概念 A/B/C"
  }，
  "parent_general_relevance": {
    "span": "文章中与父节点一致的内容",
    "reason": "比如：属于父节点主题的哪些部分"
  }，
  "candidate_for_new_node": {
    "span": "文章中与父节点一致但不属于兄弟节点的内容",
    "reason": "比如：符合父节点主题但与兄弟节点都不重复"
  }，
  "new_node_summary": { # 基于 candidate_for_new_node 生成摘要
    "explanation": "这一新类讨论的是什么",
    "scope": "这一新类的范围是什么",
  }
}
根据以上内容重写prompt，然后对应修改解析输出的代码。真正的summary还是new_node_summary中的内容，因此分类系统不需要改。

对于构建生成系统训练数据，做如下修改：
1. 在构建生成系统数据时，不再一次性生成多个新类，也就是说，在构建第一类、选择正确输出删除时，只删除一个。构建第二类、输入子节点summary为空时不用变，因为输出后面会改。
2. 现在生成数据的方法大体不用变，只需要修改输出即可。目前是直接将正确输出结果的summary作为输出，现在改为将构建好的summary直接输入deepseek_api，然后解析其输出。如果能解析成功，就把原输出作为数据集的输出。
3. 在生成数据集数量达标时立刻停止，不能浪费api的额度。


回答你的问题：
1. 是对的，不过要注意，作为训练数据的completion是api返回的完整的原输出，不能只把解析出来的summary作为输出。以防api返回无关内容，请在prompt中说明。
2. 直接使用我们新写的生成系统的summary即可。
3. brother_overlap为中的键值为空即可。比如输出"span"： "None", "brother_node": [], "reason": "兄弟结点为空，没有内容"。类似这样就行。当然了，要输出英文，不要中文。
4. 目前的生成系统也只生成了这两个字段，full字段是在保存结果的时候将那两个字段拼接起来的结果。修改后，将这两个字段提取出来以后，后面的处理步骤不用变，依然拼接作为full，依然用full作为一个节点的summary。
5. 直接提取原文片段。
6. 在循环中实时检查数量，达到目标就break。只需要在生成数据集这样做。
7. 解析失败就跳过。


目前，生成系统的prompt中，若父节点没有summary（根节点），在构造训练数据时会用topic填充。但推理时好像是空。将推理时的处理修改，与训练数据同步。


目前整个项目的效果不错。分类系统能够比较正确的分类、判断是否需要新类；生成系统能够生成具有区分度的、具有层次的总结。最终得分也很高。
但是只有一个问题，就是目前生成的类粒度太细。这是整个项目在当前设计下必然会导致的问题。
1. 我需要加入第三个系统，合并系统。这个系统输入的是两个类的summary和topic，输出是二分类判断，判断这两类是否应该被合并。你需要设计一个prompt，输入大模型，让大模型输出判断结果。
2. 我们需要训练这个合并系统。
首先是构建训练数据集。方法是，像现在构建数据集的方法类似，先读取structures_file。数据只在同一topic下构建。对每个topic的结构树，按以下方法构建数据集：
  1. 第一层的两个节点对构成负样本。
  2. 不同层之间的节点对构成负样本。
  3. 第二层及以后，同一层但是不同父节点的节点对，构成负样本。
  4. 第二层及以后，同一层且相同父节点的节点对，构成正样本。
以此构建合并系统数据集，按照与现在一样的sft数据集格式。
3. 和现在的训练方法一样，用sft训练这个系统。
4. 在推理步骤之后，加入一个合并步骤。读入推理的结构树，对每个topic进行单独处理。对每个结构树只处理第一层节点，把第一层节点两两输入合并系统，判断是否应该被合并。
这里的推理也要用vllm实现，先收集所有的prompt，统一推理，再统一处理结果。
把每个第一层节点视为图上的一个点，合并系统判断为True的两个节点之间有权重为1的边，然后应用AP聚类算法生成聚类，然后把一个簇的节点放在一个新的父节点下。如果一个簇中只有一个节点，那不用改变。

这个流程要融入run.py中，参数加入config之中。


回答你的问题：
1. 最简单的格式即可，这只是一个简单的二分类问题。
2. 所有同父节点的兄弟节点对都是正样本。但你后面的理解不对，虽然所有同父节点的兄弟节点对都是正样本，但只是第二层及以后的，第一层的节点（比如"History"和"Geography"）都是第一层的节点。
这里要注意，所有的样本对都必须是同一topic下的。也就是说，topic是结构树的根节点，"History"和"Geography"这种是第一层，再往下才会构建出正样本。
这里要注意正负样本的平衡。我会限制数据集的整体大小，在此基础上进行sample。
3. 上面讲过了。
4. 先生成20000样本，正负比例1:1
5. 抱歉这里我说错了。这是一个图的连通分量问题，采用Leiden算法吧。参数放在config之中。这图的权重只有0和1，你应该能很容易的选出合适的参数。
6. 新的父节点summary先用一个编号填充即可，比如“New category i”这种。


我需要你将整个项目按以下要求修改：
1. 前两步，也就是数据划分和生成summaries不变。
2. 删掉分类系统，将分类系统的功能合并进生成系统，构成分类生成系统。现在生成系统的输出中有一个brother_node字段，这个字段本身就完成了分类的任务。
合并后的分类生成系统可以生成多个新类，新类的结果改为列表形式，其余不变。
相应的调整：如果文章与所有的子类都不相关，在brother_node字段输出空值。如果当前没有子类，模型觉得也不需要再细分子类，则在最后的新类也输出空值。
3. 删除合并系统。
4. 添加一个总结更新系统。
这个系统输入分为五部分，第一是topic，第二是一个节点的summary，第三是这个节点的父节点summary，第四是这个节点的兄弟结点summary，第五是一篇文章或者一段summary。
第五部分输入的summary是新加入第二部分输入的节点的，新加入的内容可能会导致这个节点的summary需要更新，因此系统需要判断这个summary是否需要更新，如果需要，输出更新的summary。
如果判断需要更新summary，新的summary要求：
  （1）只关注和topic有关的内容。
  （2）更新后的summary必须和所有的兄弟节点summary没有交集。
  （3）更新后的summary范围必须在父节点summary之下。
  （4）根据第五部分输入的内容，在原有的summary基础上进扩展。
系统通过大模型来实现。大模型的输出格式要和现在的生成系统类似，先输出第五个输入和第二个输入节点以及兄弟结点相关的内容，再输出属于父节点，但是不属于上一部分输出的内容，再输出更新的summary。如果不需要更新，输出特定值。
5. 对应的修改构建系统。新的构建流程为：
  （1）对每个topic都构建独立的结构树，先初始化一个根节点，summary为空。
  （2）依次处理文章，对一篇文章，从根节点开始作为当前节点，将topic、子节点summary、当前节点summary、文章输入合并后的分类生成系统，将文章分到子节点，生成、加入新类（如果分类生成系统生成了新类）。然后再递归的处理分类到的子节点，直到这篇文章处理完。
  （3）当一篇文章被分类到一个或者多个叶子结点以后，对于分类到的每个叶子节点，将topic、当前节点summary、父节点summary、兄弟结点summary和文章输入总结更新系统，判断是否需要更新这个节点的summary。
  （4）如果一个叶子节点被判断需要更新summary，则用总结更新系统输出的summary生成一个新的节点，替代当前叶子结点的位置，然后将叶子节点放在新节点之下。然后往上的父节点也需要判断，输入为topic、父节点的summary、父节点的父节点summary、父节点的兄节点summary和刚更新的summary，判断父节点是否需要更新summary。如果需要，再往上递归判断，直到根节点或者不更新。注意，只有叶子结点被判断需要更新时，需要加一个新节点。不是叶子结点，就直接把新的summary换进去即可。
举个例子，当前的树为：
T
  A
    a1
    a2
    a3
  B
    b1
    b2
  C
  D
那么对一篇文章doc，先把ABCD、doc、T输入分类生成系统，加入返回的结果是：分类到AB，生成新类E，则生成新类E，doc被分到ABE。然后对ABE节点再重复上面的分类过程。
假如doc最终被分类到了a1、a2、b1，那么将T、a1的summary、A的summary、a2a3的summary、doc输入总结更新系统，如果输出a1需要更新，新的summary是aa，那么就构成A - aa - a1 的父子关系，然后继续判断A是否需要更新。如果A需要更新，用新的summary替换原来A的summary即可，不需要加新节点。
6. 数据集构建。
6.1 分类生成系统的数据集构建。构建方法是现在两个数据集构建方法的融合。
在构建数据集输入时，方法与现在的分类系统相同。构建四种类型的数据，比例2:4:1:1，第一层和后续层的数据比例为3:7。
小改动为：生成第一类数据时，可以删除多个正确类别，但请保证删除多个类别的比例很低。
构建数据集输出的方法与现在的生成系统相似，都是用大模型api来生成。区别在于，现在是直接用生成系统的prompt交给api得到结果。而现在，你需要在原有的prompt上说明，我们已经确定输出的brother_node和新类summary，让api在已知这些输出的情况下，补缺其他输出。
而我们确定的输入则是现在分类系统的确认方式。brother_node和新类，对于第一类数据是没被删除的类编号和被删除类的summary，第二类数据是正确的类编号和空，第三类是空和空，第四类是空和正确类的summary。
我们将api返回的补全后的完整输出作为数据集的输出。
6.2 总结更新系统的数据集构建。构建方法是，先训练分类生成系统，然后用训练后的分类生成系统，用构建系统来对train topic构建结构树。在需要总结更新系统输出结果时，用大模型的api来推理结果，然后解析结果，将结果返回构建系统，继续构建。
同时将api返回的合法完整输出保存起来，分为两部分，判断需要更新的和不需要更新的输出分别保存，然后按总数和比例采样，得到总结更新系统的数据集。
这里要注意，你需要先计算两个部分需要多少数据，然后计算每个topic对每部分需要生成多少数据，再动态的调整采样个数。比如你计算每个topic需要生成70个第一部分数据、30个第二部分数据，那么在这个topic采集对两部分都采集够以后，立刻退出这个topic的结构树构建，处理下一个topic。如果上一个topic到结束也没有采集足够的数量，那么这个topic就增大采集个数，补上亏空，在数量达标后也要立刻退出。
我需要通过这种方式保证数据topic的多样性，又不浪费api的额度。
7. 推理时，一个显卡加载分类生成系统，一个加载总结更新系统。在生成总结更新系统的数据集时，两个显卡都加载分类生成系统。

以上的所有要求，如果一个代码修改的很多，可以放心的直接重写。参数管理依然使用现在的方式，对应修改即可。

回答你的问题：
1. 我觉得采用现有的JSON格式即可，只需将new_node_summary改为列表。
2. 采用第一种吧。不过前面其他输出的推理什么时候都不能少，要先输出第五部分输入与第二四部分输入的重合部分，再输出和第三部分重合、但与二四不重合的部分，再判断是否需要更新以及更新的summary。
3. 10%是可以的。总结更新系统二者的比例先定为1:1吧。总结更新系统的样本总数先设为10000。这些都是config的一部分，我后续可能会修改。
4. a是对的，但是我忘说了一点。新节点summary是更新后的summary，原叶子节点保留原summary和citations（这里要把目前处理的文章排除掉），新节点取代原叶子节点的位置，而原叶子结点作为新节点的子节点。到了这里需要再加一步，将新节点、文章、原叶子节点再输入一次分类生成系统，给文章再分一次类。
按上面的例子来说，总结更新系统判断需要更新summary，得到了A - aa - a1的结构以后，还需要将aa作为当前节点，把文章和a1输入分类系统，判断文章是否属于a1、是否需要在aa下添加其他节点。这里的叶子节点就不需要判断是否更新了。
5. 我在第243行的描述确实不严谨，没有考虑输入的顺序。实际上分类生成系统要求的输入有当前的topic、当前节点的summary、当前节点的子节点的summary列表、文章内容。对应的243行严谨表述为：输入T、空、ABCD的summary、doc。
6. 是的，驶入的输入的"兄弟节点summary"是a2和a3的summary列表。第二点也是的，输入的"兄弟节点summary"是B、C、D的summary列表。不过严谨来说还有E，在例子中E是新生成的类。

回答你的问题：
1. 这样的输出格式没有问题。
2. 完全正确。
3. 是第二种情况。如果某个父节点判断为"不需要更新"，就停止往上递归。
4. 我想采用第三种策略。由于国建数据集时需要分类生成系统和api的推理，速度很慢，因此需要提高并行度。我的想法是，类似于现在的推理一样采用多个线程处理，每个线程内对topic进行单次遍历，后面的topic补前面的亏空，直到总数达标。
5. aa、a1、b1都加。如果我没记错的话，现在的结构树中中间节点的citation字段会保存所有子节点的citation，因此在这里应该采取统一的策略，在aa、a1、b1都加。不过还是请你检查一下我有没有记错。
6. 是的，会明确的告诉API这两个答案，但不止补充reasoning，还要补充spans。也就是除了这两个答案是我们给定的，其他的都要补充。

我发现我想错了。现在写的代码是在叶子节点被判断总结需要更新以后往上递归，更新父节点。但实际上，一个节点的子节点summary有改变时就需要更新了。
所以正确的逻辑应该是，前面的逻辑不变，文章被分到叶子结点时，对把叶子结点输入总结更新系统，如果需要更新，就新建节点、再运行一次分类。
接下来是，如果一个summary改变了那么就需要判断父节点是否需要改变。这样改和以前的差别是，对于刚创建的新类，不管这个新类的summary是否需要更新，其父节点都需要进行检查。


1. 我觉得你计算每个topic需要采用的样本个数时有错误。第一个topic算出的个数是正负样本各110条，这明显是平均个数的一倍。
2. 我发现在构建树的最开始，把一篇文章和根节点输入分类生成系统时，系统有时会判断达到叶子结点。这很明显不对。请检查一下分类生成系统的数据集构建。
这种情况应该属于第四类数据，也就是在一个新节点下生成类别，但是模型却判断为第三类数据，也就是把根节点当成了叶子结点。请检查一下是不是第三类数据的生成逻辑有问题？
3. 在构建树时有一些逻辑问题。在一个新类生成时，这个新类就不需要判断是否需要更新summary了，直接把它视为更新后的summary即可。
完整的逻辑应该是，一个文章被分类到现有的叶子节点中以后，对这些叶子结点检查是否需要更新。然后将所有的新生成的类和更新的类放在一个列表中，视为变化的类。对每个变化的类，检查其父节点是否需要更新summary。


问题2没有那么简单。在构建第三类数据时，程序是在目标结构树上构建，而不是模拟构建结构树的过程。也就是说，不会出现根节点下没有子节点的情况。
请你写一个脚本检查一下当前的数据集，看看在现在的数据集中是不是存在输入根节点、输出空和空的情况。


你在构建结构树时的代码有问题。当一个文章被分类到一个第一层节点，并判断这个节点为叶子结点后，这个节点下凭空出现了一个summary与这个节点一模一样的节点。
此时还没有进行向上递归的更新判断，那就是在对叶子节点判断是否需要更新的部分出了问题。

1. 在总结更新系统的prompt中说明，更新后的summary要限制长度。
2. 在构建树时，文章被分类到叶子节点且判断需要更新以后，加一个判断：如果当前节点的深度没有到限制的最大深度，则用更新的summary创建节点。如果已经是最大深度，直接把叶子节点的summary更新。其他操作不变。
3. 现在是串行的处理topic，我需要你用多线程，每个线程内采用现在的方法。相对应的，你需要将数据采样个数平均到每个线程内。


对于总结更新系统的数据集，现在的模拟构建结构树的生成数据方式速度实在太慢，我选择用一个更粗糙但是速度快很多的方式。
请你保留现在的模拟生成方式，加一个如下的数据生成方式。
对一个topic，先读取它的目标结构树，生成以下两种数据：
1. 对叶子节点和这个节点下的一篇文章构建prompt。输入为：
  topic_name是当前topic。
  node_summary是这个叶子节点的summary，这个summary以一定的概率进行扰动，扰动就是从summary中的描述或者范围中删掉一句话。这句话不能是第一句、不能过长。
  parent_summary是叶子节点父节点的summary。
  sibling_summaries是叶子结点的兄弟节点summary。
  new_content是这篇文章。
2. 对中间节点和这个节点下的一个子节点构建prompt。输入为：
  topic_name是当前topic。
  node_summary是这个中间节点的summary，这个summary以一定的概率进行扰动，扰动就是从summary中的描述或者范围中删掉一句话。这句话不能是第一句、不能过长。
  parent_summary是中间节点的父节点的summary。
  sibling_summaries是中间结点的兄弟节点summary。
  new_content是这个子节点的summary。
按照要求的总数×2的个数和比例生成这两类数据。比如要求数据集有10000条，比例1:1，那么就生成两类数据各10000条。这多出来的是为后续采样留余地。
将所有的prompt一起输入api，对返回的结果进行解析，将失败的结果抛弃，成功的结果分为需要更新和不需要更新两类。现在有四类数据，第一类更新、第一类不更新、第二类更新、第二类不更新。
最后按照总数和两类数据的比例、需不需要更新的比例进行采样，得到最终数据。

在扰动时，summary分为两个字段，扰动就是从一个随机字段中删随机一句符合要求的句子。这个句子是指用逗号或者句号隔开的短句，不是一个完整的两个句号之间的长句子。对长度要有限制。


回答你的问题：
1. 两类数据的比例是这Type1 vs Type2 的比例。这两个比例都配置参数，先设为1:1。
2. 作为配置参数，先定为50%
3. 不要超过某个token数，比如5。如果没有这样的短句，那就不扰动。
4. 这个第一句是两个字段的完整第一句，也就是这两个字段的第一个句号之前的内容都算第一句。
5. 用命令号参数指定。
6. 不需要分批，api内部会自己处理的。使用run_prompts_to_texts方法即可。

问题不在这里。p先基于A更新再基于B更新是没有问题的，因为更新只是内容扩展，不会删除内容。
是不是prompt的说明不够清晰？我让模型推理了三部分内容，第一部分是新内容与父节点、叔父节点重叠的部分；第二部分是与祖父节点重叠但与第一部分不重叠的部分；然后把第二部分的内容更新到父节点的summary之中。是不是这个逻辑没有说清楚？


这次的推理结果中大部分的topic的OMNI得分都比较高，但是有两个的得分远低于其他的topic。推理的结构树汇中也没有明显不正确的地方。
请你对比一下推理结果与真实结果，分析一下为什么这两个topic会分低


1. 在第二部分中，我的方法是overlap的，分类更新系统没法一次只输出一个结果，那我如何进行平面聚类性能对比和层次聚类对比呢？那些我要比较的方法没法overlap啊
2. 消融实验中，我如何去掉分类和生成呢？去掉这两个直接没法进行分类了啊
3. 我能不能跳过效率分析这一步？我的方法推理很慢，我不想展示这个缺点可以吗？


我生成数据的过程如下
1. 选10个类别，每个类别选出10个topic。
2. 对每个topic，爬取其Wikipedia页面并解析，得到文章的结构、段落和每段的引用文章。
3. 对每个topic的每个小标题，用bing进行检索，得到与这个小标题有关的文章。
4. 对所有的引用文章和检索文章，用性能最好的大模型来判断，文章除了其所属的标题，还是否属于其他标题。以此一篇文章属于多个子标题。
5. 从每个类别随机选一个topic，共10个topic，其对应的结构树和文章作为测试集。剩下的90个topic中，10%的文章作为验证集，90%作为训练集。
6. 用你知道的这个方法构建分类生成系统的数据集，包含四类数据。
7. 训练好分类生成系统以后，运行整个构建过程，用性能最好的大模型来模拟总结更新系统的结果，构建总结更新系统的数据集。

我现在需要在我的实验计划中说明整个数据集的构建过程、数据集的范围以及为什么这样的数据集是合理的。


我现在需要对整个项目做一个比较大的改动。不必在意与现有的方法兼容，如果某一部分改动很大，不方便兼容现有方法，可以不兼容，直接删掉重写。
1. 重写分类生成系统，现在更名为分类系统。系统输入不变，输入topic、当前节点summary、子节点summary、当前分类文章。不再需要输出思考过程，只需要输出分到哪一类、是否需要创建新类。
这个输出比较特别，需要每一行对应现有的一类，最后一行对应NEW，输出Yes/No判断是否属于这一类、是否需要NEW。也就是多个二分类的整合。
允许多个Yes，也允许现有类 Yes + NEW Yes
2. 重写总结更新系统，现在更名为总结系统。系统输入不变，输入topic、当前节点summary、父节点summary、兄弟结点summary、新加的内容（一篇文章或者一个节点summary）。
也不再输出思考过程，直接输出是否需要更新当前节点summary、更新的summary（如果需要更新）。
输出空间为(False, None)和(True, updated_summary)
3. 构建系统。构建过程彻底改变，比现在更加简单、直接。
先定义状态（State）为（当前结构树T，文章D，当前节点N，action A）。两个模型的每次输出都是一个action，这样每一篇文章的构建过程就会形成一个由state构成的轨迹。state不会全部输入给分类、总结系统，这两个系统还是只需要各自的prompt输入。
构建过程如下：
（1）一篇文章，自根节点开始作为当前节点，开始分类。
（2）输入分类系统，输入分类系统，在「已有子类 ∪ NEW」中做决策。
（3）如果分类系统判断需要生成新类，则将把topic、空、所有子节点summary、文章输入总结系统，生成一个summary，作为新类，把文章分进去。
（4）如果当前节点没有子节点、且分类系统判断需要生成新类、那么在第3步执行完以后，将当前节点下所有的文章和当前节点、新的子节点重新输入一次分类系统，确保之前的文章不会停在中间结点。这个分类只需要一次即可，让中间节点的文章往下走一层。
（5）递归执行2、3、4步，直到文章被分到叶子节点、且判断不需要新类生成。
（6）对于这篇文章分类到的叶子结点，从叶子结点向上递归。将topic、当前节点summary、父节点、兄弟结点summary和改变的子节点summary或者新加的文章输入总结系统，输出是否需要更新当前节点和更新的summary。如果不需要更新或者当前节点为根节点，停止递归。
伪代码如下：
def classify_and_update(topic, article x, root r, taxonomy T):
    current_node = r
    path = [r]

    # ---------- Top-down classification ----------
    while True:

        children = current_node.children

        # Step 2: classification decision
        action = ClassificationAgent(
            topic = topic,
            article = x,
            current_summary = current_node.summary,
            children_summaries = summaries(children)
        )

        # Step 3: create new class if needed
        if action == NEW:

            new_summary = SummaryAgent.generate(
                topic = topic,
                current_summary = "",
                parent_summary = current_node.summary,
                sibling_summaries = summaries(children),
                changed_children_summaries = x
            )

            new_node = Node(summary = new_summary)
            current_node.add_child(new_node)

            # Step 4: re-route existing articles if current_node was leaf
            if children == ∅:
                for doc in current_node.articles:
                    child = ClassificationAgent(
                        topic = topic,
                        article = doc,
                        current_summary = current_node.summary,
                        children_summaries = summaries(current_node.children)
                    )
                    move(doc, from_node=current_node, to_node=child)

            next_node = new_node

        else:
            next_node = action  # existing child

        path.append(next_node)

        # Step 5: termination condition
        if next_node.is_leaf() and not requires_new_class(x, next_node):
            next_node.add_article(x)
            break

        current_node = next_node

    # ---------- Bottom-up summary update ----------
    for node in reversed(path):

        if node.is_root():
            break

        update_flag, updated_summary = SummaryAgent.generate(
            topic = topic,
            current_summary = node.summary,
            parent_summary = node.parent.summary,
            sibling_summaries = summaries(node.siblings),
            changed_children_summaries = summaries(node.children)
        )

        if not update_flag:
            break

        node.summary = updated_summary

    return

在构建时，要多topic并行。跨topic，先收集够一定数量的两个系统的prompt，然后一次性给模型，提高推理的并行度。为了防止最后几个prompt留在池中永久等待，设置一个最长等待时间，超过这个时间就直接拿去推理。这样可能会快一些。

4. 生成数据和训练。这一部分需要完全重写。
（1）准备组件。我们有了分类系统和总结系统，现在还需要一个标注系统，用于标注一次分类正确的答案是什么。输入是topic、当前节点summary、子节点summary、文章和这个文章的所有正确路径（topic - title - subtitle这个），让线上强大模型根据正确分类路径判断应该将文章分到哪一类或者是NEW。这个标注系统实际上是把已有的正确标签和现有的summary对齐。标注系统可以什么都不输出，表示这里面没有正确结果。
（2）轨迹采样。对每篇文章先运行一次完整的构建流程，区别在于在每次大模型输出时采样多个结果，然后对结果去重、解析是否合法、取top_k。如果得到了多个输出，那么轨迹在这里分叉，对每个分支都继续执行构建。最终得到一个状态树，每个节点是一个状态，每个从根节点到叶子结点的路径都是一共轨迹。
（3）轨迹级全局 reward 计算（R_global）。对每一条轨迹 τ，在轨迹最后一个状态的结构树T上，将文章D重新进行一次分类。冻结参数，只用分类系统，任何情况下不调用总结系统。然后每次分类结束时，用标注系统得到这次分类的正确结果和错误结果（每篇文章都记录输入输出，避免重复启动标注系统，浪费额度和时间）。
对每一条轨迹 τ，每一次分类，s(c)=logP(Yes∣article,summary(c))，R_margin​=max_{c\inC+}​s(c)−max_{c\notinC+}​s(c)，将所有的R_margin相加得到最终的R_margin。R_len​=-∑_{t=1}^{∣τ∣}​α(a_t​)。R_global = R_margin + R_len。
​α(a_t​)为，α(classify(existing))=1，α(classify(NEW))=β>1，α(summary_update at depth d)=γ/d​
（4）轨迹排序与偏好对构建。对所有的轨迹，按照R_global排序，两两构建偏好对，R_global大为正轨迹，小的为负轨迹。
（5）从偏好对中抽取分类模型的action子列，用trajectory-level DPO训练分类模型。从偏好对中抽取总结模型的action子列，用trajectory-level DPO训练总结模型。
（6）用更新后的模型继续，循环执行2-5步。
伪代码如下：
for each training iteration:
    trajectory_buffer = []

    for document x in D:
        # ---------- Step 1: Trajectory Sampling ----------
        # 这里演示的是循环多次构建，如果不方便一次性采样多个回答就用这种方法。如果能够一次性采样多个回答，就不用这种方法了，一次性采样更高效
        trajectories = []
        for i in range(N_samples):
            T = initial_tree()
            actions = []

            # Classification phase
            node = T.root
            while True:
                candidates = π_cls.sample(x, node, T)
                action = sample(candidates)
                actions.append(action)

                if action == NEW:
                    new_summary = π_sum.generate_new_node(x, node, T)
                    T.add_child(node, new_summary)
                    node = new_summary
                else:
                    node = action

                if node.is_leaf() and not action.requires_new:
                    break

            # Summary update phase (bottom-up)
            for v in reversed(path_to_root(node)):
                update = π_sum.sample_update(x, v, T)
                actions.append(update)
                if update.apply:
                    T.update_summary(v, update.summary)
                if v.is_root():
                    break

            trajectories.append((actions, T))

        # ---------- Step 2: Global Reward ----------
        for τ, T_final in trajectories:
            scores = {}
            for c in T_final.categories_with_NEW():
                scores[c] = entailment_score(E, x, c.summary)

            C_pos = E.judge_correct_categories(x, T_final)
            if C_pos is empty:
                R_margin = -gamma
            else:
                R_margin = max(scores[c] for c in C_pos) \
                         - max(scores[c] for c in scores if c not in C_pos)

            R_len = -sum(alpha(a) for a in τ)
            R_global = R_margin + R_len

            trajectory_buffer.append((τ, R_global))

        # ---------- Step 3: Preference Construction ----------
        preference_pairs = rank_and_pair(trajectory_buffer)

    # ---------- Step 4: Training ----------
    for (τ_pos, τ_neg) in preference_pairs:
        π_cls.update(classification_actions(τ_pos),
                     classification_actions(τ_neg))

        π_sum.update(summary_actions(τ_pos),
                     summary_actions(τ_neg))


回答你的问题：
1. 采用选项B，多行Yes/No格式，每行是一个类别的结果，最后一行是NEW的结果。
2. 标注系统用DeepSeek API，采用modeling/deepseek_api.py的run_prompts_to_texts方法即可。需要你写一个prompt模板，和现在的prompt放在一起。格式你自己决定，你选择你觉得效果最好的格式。
3. 我这个是需要采样路径、生成偏好对，紧接着就是模型训练，所以应该不能用vllm加载模型。你优先考虑如何实现trajectory-level DPO，确定模型的加载方式，然后用高效的方式进行采样。
4. 分类系统不是输出了多行嘛，P是每一行答案（Yes/No）位置的Yes的概率。至于如何获取这个概率，则需要根据你加载模型的方式来决定。
5. 如果有现有的trajectory-level DPO库可以用，直接使用即可。如果TRl的DPOTrainer可以实现trajectory-level DPO，那直接用就行。
6. β先设为2，γ先设为2，还有一个忘说了，R_global = R_margin + R_len是不对的，他们不能直接相加，需要一个参数，R_global = R_margin + λ * R_len，λ先设为0.1。这些都加到config之中，方便后续调整。
7. 批次大小先设为32，等待时间设为1秒吧。这两个也要加到config中

回答你的问题：
1. 首先，这个系统更名为总结系统，它不止是总结更新了，在分类系统输出NEW时，也要用它来写总结。输出格式也要改为简单的格式，你的这个格式就不错。
2. 采用选项A，自己实现trajectory-level DPO。请你确保这部分的代码正确，必要时可以使用浏览器检索信息，因为我不是很懂强化学习，所以这段代码我很难debug。
3. 我觉得可以采用你的这个方法，不过要注意，我们的采样数据和训练时交替进行的，因此你在训练结束后需要合并、保存模型，然后用vllm加载训练后的最新模型，然后继续推理。
    说到这里我想起来我忘说了，在一个topic的一篇文章采样完轨迹以后，不是要把文章在每个轨迹最后的结构树上重新分类一次嘛，这次分类不要真的把文章保存到节点中，这样文章会重复。获得数据后，下一篇文章需要在得分最高的那一个轨迹的最后的结构树上继续。
    采用你的这个方法以后，我们就需要尽可能的减少切换的频率。因此，在训练的前期，由于是冷启动，需要batchsize更小，先设为300，在中期变成1000，在后期变成3000。这里的batchsize是指采样的文章格式。对于每篇文章的轨迹，只生成Top-1 vs Rest，也就是4条轨迹，只构建得分最高的1个和其余3个，共三个偏好对。
4. 是的。如果采用你在上面说的方法，用vllm加载模型推理的话，自然也就不需要冻结参数了，直接推理计算reward即可。
5. 如果这里面有你能用得上的代码你当然可以留着，不过我建议你直接重写吧，估计也没有太多你能用得上的。
6. 标注系统采用格式A就行，因为这里不涉及对每个类别的logit计算。


在采样的代码中出现以下问题：
1. 文章的选取有问题。首先你要知道，在同一个topic下，文章是串行、一个接一个的在结构树中分类，并更新结构树的。也就是说，一篇文章会更新结构树，并影响后续文章的分类。
你现在是直接随机选择一堆文章，然后在采样开始时直接重新建一个新的树，这完全不对。你应该对每个topic都保存一个树，一开始是新建，然后这个topic下的文章就在这颗树下进行分类，并更新树，
比如我现在train topic有90个，我需要采样300篇文章，那么你就先把每个topic的文章打乱，并为每个topic初始化一个树。然后每个topic取第一篇文章，处理这90篇文章，每个topic的树保留下来。再从每个topic取第二篇文章，继续在这树上进行分类更新。直到文章数量够300为止。
这一点之前说过，我再强调一遍。在处理一篇文章的时候，我们会得到多条轨迹，并且为每一个轨迹计算一个R_global，我们会保留得分最高的那一个轨迹的最后一个状态的结构树，作为后续的结构树。在计算R_global时，会把文章重新分类，这一分类过程只用于计算得分，不会真正的分类，也不使用总结系统。
这个树会在多次迭代中继承。比如我第一个迭代中为每个topic得到了一个结构树，然后我去执行训练。在训练结束，开始下一个迭代时，仍需要在这个结构树上继续进行。也就是说，你需要保存结构树、下一个该选哪篇文章了。
当一个topic所有的文章都被处理过以后，将结构树重新初始化，然后重新打乱文章，继续操作。
2. 你在采集轨迹的时候没有按我说的用并行的方法。比如我现在有90个train topic有90个，那应该是90个topic根据需要将prompt放到分类系统或者总结系统的prompt池中，然后等待结果。90个topic并行处理。而不是处理完一篇文章后处理下一篇。
在维护两个系统的prompt池时，要注意返回对应的结果。也就是说，你需要给每个prompt一个id，结果池中用id作为索引，让每个topic能够正确的拿到属于自己的结果。

你的理解大致是对的，我说的更详细些，方便你理解。
1. 确实需要添加树状态管理。在这里你需要注意，在处理一篇文章时，输入的树为T0，然后在T0上执行构建流程，每个模型输出多个结果，构成分叉，然后每个分叉都继续走下去，构成一个action树。在这里你要注意，这个树不能乱。也就是说，在多个轨迹中，你不能只用一个树对象，只用一个会导致所有的action都融合在一个树中。
也就是说，每篇文章构成一个构成一个action树，这个树从根节点到叶子节点的所有路径都构成一个轨迹，每个轨迹到最后都有一个独特的结构树。
3. 这里要实现一个prompt采样多个结果，配合上面的逻辑。


1. 在你批量采样轨迹时，采用的是限制迭代次数的方法，不要这样做，请你直接限制树的深度。当树的深度达到最大值时，停止继续分类，直接运行总结更新，也能避免无限循环。
2. 你可能是忘了我之前的要求，我再给你说一下。在每个系统的prompt池中，要先收集够一定数量的两个系统的prompt，然后一次性给模型，提高推理的并行度。为了防止最后几个prompt留在池中永久等待，设置一个最长等待时间，超过这个时间就直接拿去推理。
3. 你说的待完成中，现在的builder.py就是你新写的完整构建流程，可能你很多代码不用重写了。

在parallel_trajectory_processor.py的362行，处理一篇文章时，你是用循环采样一定个数的轨迹，这不对。应该是，按照正常的构建方法，只是每个系统都输出多个答案。当输出了多个不同的、合法的答案时，自动进行分叉，从而构建出action树。
我也不希望直接限制每篇文章的轨迹数量，因为有的文章就是能生成很多轨迹，有的文章只能生成一个轨迹，完全无法对比。我想只通过限制每个系统输出的答案数量和取用数量来间接限制轨迹数量。比如我让模型生成5个答案，过滤掉重复的、非法的，选取两个，用这种方式来限制。
而你现在是直接循环，这样有可能轨迹是重复的，轨迹的前半段会重复，也浪费时间。
我觉得是你轨迹和分支的理解出错了。372行，你最外层循环是采样轨迹的个数，而下面是把分支走完，这是错的。用你的例子来说明：
初始树 T0
    ↓
[分类模型采样3个结果] → Action1, Action2, Action3
    ↓           ↓           ↓
   T1a         T1b         T1c  (三个独立的树副本)
    ↓
[继续分类] → Action1.1, Action1.2
    ↓              ↓
   T2a           T2b
   ...           ...
这样一个带有分支的action树，就能生成多条轨迹，比如Action1,Action1.1...这就是一个轨迹。同样的Action1,Action1.2...Action2,Action2.1...这样生成多个轨迹。


"NEEDS_UPDATE: No\n\n**Explanation**: The new content is fully incorporated into the current node's summary, which already provides the foundational biography of Charles Darwin, including his birth, family background, and early education. The new material does not introduce any aspects of Darwin's life or work that are outside the scope of the current node's established content.\n\n**SCOPE**: The scope remains focused on Charles Darwin's personal history, including his birth details, family background, and early educational experiences, as covered by the original summary. It does not include his scientific theories, career, or later life events."

我发现我的任务设置和轨迹级dpo的假设差距很大，因此我需要改成用iw sft来训练模型。
训练方法说明.md 是一个详细的、完整的设计说明，请根据说明修改代码，将代码改为iw sft来训练模型。

我需要你做如下改动：
1. 取消iteration之间的状态继承。后面iteration不能因为前面得到的不好的结构树而受到影响。也就是说，现在应该是，对每个iteration，打乱文章、新建一个初始结构树，然后其他逻辑不变。
2. 我需要你扩展标注系统的功能。现在的标注系统是把真实路径和子类进行匹配，这只引导了分类系统，没有对总结系统进行限制。现在总结系统不再需要输入文字内容，只需要输入topic、当前节点summary、子节点summary、真实路径。
  然后需要它先判断是否有子类的summary超出了当前类别summary的范围，输出子类别编号或者None。然后判断现有子类summary两两之间是否有重复，输出编号或者None。最后再把真实标签和子类进行匹配，也就是现在的功能。
  现在的标注系统用在计算Reward时，当标注系统判断超出了范围，或者有重复，给一个惩罚。


我需要你做如下改动：
1. 我需要你修改现在的程序流程。现在的程序逻辑是没问题的，但接下来我需要批量调用标注系统，因此流程要做修改。
  现在是先对一篇文章采样轨迹，然后对每个轨迹，用分类系统在新结构树上重新分类，然后用标注得到每次分类的正确分类结果和summary总结的评价，计算reward。
  在这个过程中，标注系统的任务与前面两步是完全分开的。因此，更清晰的做法是，按照现在的逻辑，先对个topic的文章采样轨迹，然后用分类系统在新结构树上重新分类。
  接下来，收集所有文章的轨迹以及对应的重新分类的record。然后退出两个模型，清理显卡，然后再对每个轨迹以及对应的重新分类record，批量得到prompt，用标注数据标注，再统一处理结果，计算reward。
2. 按照上面修改的程序流程，你还需要实现标注系统的批量输入和输出。这次不再是一个topic的一篇文章，而是很多topic的很多文章、对应的很多重新分类record。其实本质上你只需要实现批量推理即可。
3. 在做完前两步以后，我需要你为标注系统新加一个模式，这个模式不再调用api，而是使用一个本地的模型。这个本地模型不是base model，把这个模型路径用一个新的参数，加到config中。
4. 最后，在标注系统的prompt中，让大模型输出推理的思维链。现在prompt的逻辑不用变，只是引导模型一步一步的按照现在的指示进行思考。

回答你的问题：
1. 保存这些内容是没问题的。并且你不用很在意保存的内容，你只需要把现在的程序中标注系统的调用和后面紧跟着的计算reward这两部分提取出来就行，你只需要保存这两部分需要的信息、和构造数据集要用的信息就行。
2. 你这个问题问的很好。我觉得还是保存一下吧，顺便加一个参数，控制是否跳过采样轨迹，便于我恢复程序。
3. 用现在的代码即可。现在采样完轨迹以后、训练模型以前，需要清理显卡，用这个代码就行。当然了，如果标注系统用了新的模式，也就是用本地的一个模型，在标注工作结束以后也要退出。
4. 一次性标注所有就行。如果是用现在的api模式，deepseek_api.py内部会自己分批次多线程。如果是用新模式，也是用vllm加载模型到两个显卡，其内部也会自己分批次的吧。
5. 你可以这样理解。这里你觉得怎么好写程序就怎么来就行，如果你觉得在外面包装好所有的prompt，直接给标注模型，这样简单的话，就可以这样做。如果你觉得让标注系统自己内部包装，这样简单的话也可以。
6. 用vllm，我有两张显卡，所以要用两张卡推理。注意在这之前两个系统是各自一个线程，各自用一个显卡，不要被这个操作干扰。
7. 这样的配置可以。
8. 这样的格式是可以的，如果能在每一步中更加细分步骤就更好了。比如在第三步Match ground truth中，我们已经说明了匹配的步骤，最好能够更全面的输出思维链，这样能保证输出结果的质量。


继续回答你的问题：
9. 这样没问题，我想说的是，之前两个系统加载模型的时候，是在各自的进程内加载，那时是通过修改环境变量的方式来指定显卡的。你写的时候要注意环境变量。
10. 可以这样清理。
11. 这样的格式是可以的。
12. 可以。
13. 这样太详细了，不需要那么多。我又看了一下现在的prompt，确实没有更详细的步骤了，那就恢复之前的设计，让它输出每一步的思考过程就行了。


1. 老师您有api的额度嘛
2. 蚂蚁那边的实习有消息嘛（这俩就顺便问一下）
3. 我现在用了一个这样一个方法。处理一篇文章的整个流程是由两个模型配合完成的，一个模型负责分类，一个模型负责写summary。对一篇文章，采样多个完整的处理轨迹，每个轨迹都包含了两个模型的各种action。我的reward是这样设计的，它由两部分构成。
一部分是R_margin，是将文章在更新后的结构树上重新分类，每次分类输出中，margin = 正确类别的置信度 - 错误类别的置信度，R_margin为所有margin的平均。这一项代表了在处理了一篇文章后，更新后的结构树有多好。如果结构树充分体现了文章内容，最终的结构树合理，那么分类模型就会更自信的将文章分到正确类别中。这一项越高代表这一个轨迹越好。
这样的设计也能避免很多问题。比如如果两个兄弟类别有重复，那么这个置信度就会降低。如果子类不能充分概括文章内容，那正确结果中就显示需要新类，就会被惩罚。
第二部分是R_len，就是用来限制轨迹的长度，越短的轨迹R_len越大。这一项是用于约束，如果长轨迹和短轨迹的效果相同，那么就偏向于长轨迹。这一项权重很小，上一项的权重很大。
然后通过reward来判定两个轨迹的优劣，然后训练。用训练后的模型再采样，然后再训练，依次循环。
您觉得我这样的方法有什么问题吗？
4. 在上面的这个过程中，我用了线上的强大模型来将文章的真实分类路径和summary进行了对齐，也就是给它一些子类summary和真实分类路径，让它判断这个真实路径对应哪个summary。我必须将正确路径和summary对齐以后才能用正确路径来监督训练，因此我用这种方式来对齐。这样的行为能接受吗？

1. 我需要一个从结构角度上来计算的loss，而不是从文章分类的角度。我现在使用的方法是，把文章进行重新分类，根据一篇文章分类的正确性和自信程度来作为reward，但是我更需要的是评价当前结构树和目标结构树之前的差距的reward。
2. 也就是说，最直接的方法是处理100篇文章，用最后得到的结构树与目标结构树计算reward。但这太复杂、action太多。我需要让每处理一篇文章都得到一个reward，方法是从目标树中截取与现在相关的子树，计算reward。
3. 在此，我必须证明，截取子树然后计算reward的方法，等同于最直接的方法。也就是，我需要证明，现在分步的reward高一定能推导出最终的reward高。


我需要的reward比较特殊。它应该只由结构得分构成。
对于第一部分，结构得分指的是这个得分反应了当前的树和gold的子树之前的差异。比如当前结构树为T_n，目标结构树为T_g，那么在T_n上处理一篇文章涉及到的子树为T_n'，这篇文章在T_g涉及到的子树为T_g'，这个结构得分应该度量这两个子树直接的差异，差异越小得分越高。
首先，它需要给次优一个比较好的得分。比如，T_g'是A - B - C - D，T_n只有一个节点A，然后生成了节点A - C，这应该有一个比较高的得分，因为这已经是当前情况下能得到的很好的一个结果了，也没有出现错误。
其次，这个得分不应该被结点个数影响到。不能因为两个子树的结点多或少影响得分。
最后，这个得分应该能惩罚结构错误。也就是当T_n'与T_g'结构矛盾以后，给予惩罚。

至于只要这一部分的原因在于：
1. “哪些文章 / 子节点被分在同一个父节点下”这件事已经被包含在结构得分中。如果一篇文章被错误分类，那么T_n'就会出现不存在于T_g'中的结点，这自然会被惩罚。
2. 你说的“不可逆结构决策”，这里面过分裂和过泛化这两个其实我能一定程度上接受。也就是说，轻微的泛化和分裂无所谓，过度的泛化和分裂也会因为T_n'与T_g'差距大而被惩罚。至于“错误的父子关系”也是如此，会直接被结构得分惩罚。

你觉得我这种想法对吗？


根据你的描述，我有两个问题。
1. 你说的子树induced subtree的定义是path 上每个节点的 siblings。也就是说，在A-B-C-D中，C的兄弟结点也是子树的一部分。在A-C中，C的兄弟结点也是子树的一部分。可在处理的前期，T_n的结点一定是不全的，那岂不是第一项得分就一定很低了。
但也不能不把兄弟结点放在子树中，因为如果不放的话，出现结构性错误时，由于文章没有经过结构错误的结点，就会导致错误结构被忽略。
首先，这个问题需要解决吗？前期reward低，后期reward高，会不会影响训练的权重？如果必须解决的话，能不能从reward计算上解决问题？我们能不能找到一个方式，能够让还不存在于T_n中的结点不影响得分？
2. 在T_n中，每个结点是由一个summary来描述的，而在T_g中，每个节点是一个title，那么就有可能出现T_n中的节点对应多个T_g结点，或者返回来。在这种一对多的对应关系下，我们如何计算reward？


在T_n和T_g对齐的细节上有很多可以完善的地方。我进行了详细定义说明，并总结了完整过程，请你检查这样是否合理。
1. 定义当前完整结构树为T_n,其结点所处语义空间为n空间；目标结构树为T_g，其结点所在语义空间为g空间。
2. 定义path为文章经过的所有结点集合，分为P_n,P_g。
3. 定义induced subtree为path上每个节点及其siblings，分为I_n,I_g。I为T的子树。
4. 定义同一空间内的关系，继承关系 u < v 和sibling关系 sib(u, v)。关系集合分为R_n,R_g。
5. 定义n空间到g空间的对应关系，一个I_n中的结点可以对应零个、一个或者多个I_g中的结点，一个I_n的结点u，对应的I_g结点集合为ali(u)。
获得ali(u)的方法为，u的父节点为U，候选集合为{children(v) | v in ali(U)}，children(v)指v下面的所有节点，包括孙子结点等等。然后从候选集合中选出0、1或者多个，组成ali(u)。ali(root_n) = {root_g}。
空对应集合 K(I_n) = {u|u in P_n and ali(u)为空}。这里指包括path上的结点，其余结点是历史遗留，不应该被重复惩罚。
6. 定义n空间关系集合中的关系的“正确”和“错误”。
n空间内的一个继承关系u1 < v1是“正确”的，当且仅当任意u2 in ali(u1),存在v2 in ali(v1) and u2 <= v2。等于也算，即u2,v2是同一个结点。不“正确”就是“错误”。
n空间内的一个sib关系sib(u1, v1)是“正确”的，当且仅当任意u2 in ali(u1)、v2 in ali(v1), 不存在 u2 <= v2, 不存在 v2 <= u2。不“正确”就是“错误”。
“正确”关系集合为Correct(I_n)，“错误”关系集合为Wrong(I_n)。
7. 计算reward。reward = (len(Correct(T_n)) - a * len(Wrong(T_n))) / len(R_n) - b * len(K(I_n))
第一项是修改你现在的reward得来，由于我符号化了两个空间关系的对应，因此这里随之调整。与之前的区别在于，没给你的reward的第二项除len(R_g)，除的是len(R_n)。
第二项是惩罚path上不存在于gold树的结点。

请你仔细检查我的定义、说明和修改，最好能一步一步的看。你觉得我这样写有问题吗？


5. 对于第5步的问题，我突然意识到，无论什么时候，都不应该把alignment限制在父节点映射之下。
比如U对应结点A，u对应结点a1、b1，这是子类范围超出父类的错误，如果把alignment限制在父节点映射之下，b1就不在u的候选集中，就会忽略掉这个错误，后续就会错误判断关系的正确性。
6.1 我觉得应该稍微严格。按你说的例子，g2与h1无关，那说明u1的范围已经超出了v1的范围，那就应该是“错误”的啊。我觉得这是很明确的“错误”而不是“不确定”。
6.2 同上，我觉得应该稍微严格。按我的定义，“错误”的就是两个类出现了范围上的重复，这也是要惩罚的。
不过，6.2我的定义确实有问题，因为我发现，两个类可能都对齐到一个类上，这不是“错误”，而是分裂。我这样修改定义：
n空间内的一个sib关系sib(u1, v1)是“正确”的，当且仅当任意u2 in ali(u1)、v2 in ali(v1), 不存在 u2 < v2, 不存在 v2 < u2。
我去掉了等于，避免我刚发现的问题。
7.1 我发现我忽略了一个问题，我们应该只考虑path上的结点延伸出来的关系。因此我做一个修改，R_n,R_g定义为其中一个结点必须是path上的结点，然后reward的第一项保持不变。这样可以吗？
7.2 我已经修改了ali(u)的计算方法，父节点为空、自己也为空说明严重错误了，那这里是不是就不用改了


我和你对6.1的理解有偏差，按这个例子来说，A-C的情况，C对应的是g1、g2，A对应的是h1，那么目标结构树是A-B-C-D，这种情况下，h1应该就是目标结构树中的A，g1、g2对应目标结构树的BC，而在目标结构树中是有B<A和C<A的。
也就是说，A-C的例子在我的设定中是“正确”的。
但是你说的允许轻微的泛化和分裂，这确实要考虑。那我设置一个阈值，当一个节点对应了多个目标节点时，只错一个不算错。这样可以吗？这样我忽略了一部分错误，换来训练数据的增加和稳定。


根据我们的讨论，我重新修改了方案说明。
1. 定义当前完整结构树为T_n,其结点所处语义空间为n空间；目标结构树为T_g，其结点所在语义空间为g空间。
2. 定义path为文章经过的所有结点集合，分为P_n,P_g。
3. 定义induced subtree为path上每个节点及其siblings，分为I_n,I_g。I为T的子树。
4. 定义同一空间内的关系，继承关系 u < v 和sibling关系 sib(u, v)。关系集合分为R_n,R_g，R中的关系双方至少有一个在path中。
5. 定义n空间到g空间的对应关系，一个I_n中的结点可以对应零个、一个或者多个I_g中的结点，一个I_n的结点u，对应的I_g结点集合为ali(u)。
获得ali(u)的方法为，当一个结点为新结点时，按如下方式计算：
候选集合为T_g下面的所有节点。然后从候选集合中选出0、1或者多个，组成ali(u)。
当一个结点继承了上一次的ali(u)时，候选集为ali(u)中所有结点的父节点以及兄弟结点。
空对应集合 K(I_n) = {u|u in P_n and ali(u)为空}。这里指包括path上的结点，其余结点是历史遗留，不应该被重复惩罚。
6. 定义n空间关系集合中的关系的“正确率”。一个关系为r，正确率为S(r)
n空间内的一个继承关系u1 < v1的“正确率”为，对于一个u2 in ali(u1),存在v2 in ali(v1) and u2 <= v2，那么正确个数加1。正确率是正确个数/总数。等于也算，即u2,v2是同一个结点。
n空间内的一个sib关系sib(u1, v1)的“正确率”为，对于一个u2 in ali(u1)，一个v2 in ali(v1), 不存在 u2 < v2, 不存在 v2 < u2，那么正确对数加1。正确率是正确对数/(len(ali(u1)) * len(ali(v1)))。
7. 计算reward。reward = sum_{r in R_n} D(r) * S(r) / sum_{r in R_n} D(r) - b * len(K(I_n))
D(r) = 2 if r_0 in P and r_1 in P else 1

这是一个小总结，请根据这个继续分析这个方案有什么问题。

一、如果我在计算ali(u)时，定义所有结点的候选集都是全体结点，那么我在理论上就很漂亮。因此我修改了方法，兼顾理论和实践：
5. 定义n空间到g空间的对应关系，一个I_n中的结点可以对应零个、一个或者多个I_g中的结点，一个I_n的结点u，对应的I_g结点集合为ali(u)。
获得ali(u)的方法为，当一个结点为新结点时，按如下方式计算：
候选集合为T_g下面的所有节点。然后从候选集合中选出0、1或者多个，组成ali(u)。
当一个结点继承了上一次的ali(u)时，候选集为ali(u)中所有结点的父节点以及兄弟结点。
空对应集合 K(I_n) = {u|u in P_n and ali(u)为空}。这里指包括path上的结点，其余结点是历史遗留，不应该被重复惩罚。
你觉得这样如何？
二、在你的边界情况1中，我发现了，实际上如果一个path上的结点的ali(U)为空，那么他所有的S(r)都必定为0，这里已经惩罚了，那我们是不是就不需要第二项了，只保留第一项，这样的reward公式更漂亮。


我发现了一个很根本的问题，可能要大幅度的修改我现在的实验了。我现在的reward设计关注了一篇文章的分类正确性和树结构，这其中更重要的是文章分类的正确性，因为reward的主要部分就是这个。但实际上，我发现更重要的其实是树结构的生成。我现在做的事情是，在一棵树上从零开始，将文章依次处理，对文章进行层次分类并生成最终完整的结构树。
我的监督信号是最终的一个完整的目标结构树，文章分布在树的叶子结点上。最straightforward的方法就是处理所有的文章，然后用得到的结构树跟目标结构树之间计算reward，然后强化学习。也就是说，我需要的是一个评价树跟树之间差异的reward。 
其次，最straightforward的方法是很难做的，因为一个轨迹包含的action太多。因此我需要让每处理一篇文章后就得到一个reward，然后在一个短的轨迹上训练。为此，我需要证明这个reward的计算方式支持greedy。
为此，我重新设计了一套计算reward的方法。
1. 定义当前完整结构树为T_n,其结点所处语义空间为n空间；目标结构树为T_g，其结点所在语义空间为g空间。
2. 定义path为文章经过的所有结点集合，分为P_n,P_g。
3. 定义induced subtree为path上每个节点及其siblings，分为I_n,I_g。I为T的子树。
4. 定义同一空间内的关系，继承关系 u < v 和sibling关系 sib(u, v)。关系集合分为R_n,R_g，R中的关系双方至少有一个在path中。
5. 定义n空间到g空间的对应关系，一个I_n中的结点可以对应零个、一个或者多个I_g中的结点，一个I_n的结点u，对应的I_g结点集合为ali(u)。
获得ali(u)的方法为，当一个结点为新结点时，按如下方式计算：
候选集合为T_g下面的所有节点。然后从候选集合中选出0、1或者多个，组成ali(u)。
当一个结点继承了上一次的ali(u)时，候选集为ali(u)中所有结点的父节点以及兄弟结点。
空对应集合 K(I_n) = {u|u in P_n and ali(u)为空}。这里指包括path上的结点，其余结点是历史遗留，不应该被重复惩罚。
6. 定义n空间关系集合中的关系的“正确率”。一个关系为r，正确率为S(r)
n空间内的一个继承关系u1 < v1的“正确率”为，对于一个u2 in ali(u1),存在v2 in ali(v1) and u2 <= v2，那么正确个数加1。正确率是正确个数/总数。等于也算，即u2,v2是同一个结点。
n空间内的一个sib关系sib(u1, v1)的“正确率”为，对于一个u2 in ali(u1)，一个v2 in ali(v1), 不存在 u2 <= v2, 不存在 v2 <= u2，那么正确对数加1。正确率是正确对数/(len(ali(u1)) * len(ali(v1)))。
7. 计算reward。reward = sum_{r in R_n} D(r) * S(r) / sum_{r in R_n} D(r) - b * penalty
D(r) = 2 if r_0 in P and r_1 in P else 1
penalty = len(K(I_n)) + sum_{u in P_n}max(0, len(ali(u)) - k)。k为超参数，一个结点对应过多的目标结点，代表这个节点过度泛化，被惩罚。




!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
两个问题，第二个要注意。
1. 我突然发现，现在的sib关系的正确率定义中，是没办法区分“分裂”和“重叠的”。
比如T_g中有一类是一个人在学校的经历。现在的树中有两个sib关系为“小学时的经历”和“高中时的经历”。这是分裂，sib关系会判定为正确。
如果这两个sib关系为“高中以前的经历”和“小学以后的经历”，这就是重叠了，sib关系也会判定为正确。
如果我们采用现在的体系，就必然无法解决这个问题，因为我们现在是通过alignment的方法，而两个类alignment同一个类时，无法判定这两个类的重叠关系。
那我能不能统一的惩罚分裂和重叠？也就是说，不论是重叠还是分裂，我都惩罚。这会不会影响greedy的有效性？

2. 训练时，越往后分裂的轨迹reward差距越小，因为差异小了。这个要注意


这里出现了大问题。
我现在整个分类的流程是，从根节点开始，分类模型判断分到哪个子类或者生成新类，分类结束以后，总结模型自底向上更新类别的summary。这整个过程是无法合并两个类的。
举个例子，比如T_g是A-B，而现在的T_n是A，接下来T_n变成A-B1，我们的reward会给出一个高分。再接下来，T_n变成A-B1、B2,我们的reward又给出了高分，但后续的处理中，B1、B2只会在不影响彼此的情况下扩张修改，永远也无法生成B。
对于我来说，我觉得最终结果把B分成了B1、B2是完全可以接受的，它们分开、合并、合并后在子类中分开，这三种情况我都可以接受，所以我设计了这样的分类、构建流程。
但这对greedy的证明是不是非常不友好？是不是我很难说明我这样做的道理？

在下面两种情况二选一，你会选什么：
1. 重新设计构建流程，使之能够进行合并等操作，进而能够进行强greedy证明。
2. 使用现在的构建流程，进行弱greedy证明。

那如果我把ali关系的正确性定义回退，变成：对于一个u2 in ali(u1)，一个v2 in ali(v1), 不存在 u2 <= v2, 不存在 v2 <= u2，那么正确对数加1。
这样两个结点alignment同一个结点会被明确惩罚，也就是，我不再区分分裂和重叠，我也不去界定过度分裂的界限，而是统一的惩罚分裂和重叠，这样会不会让我的理论更优美？
实际上，仔细一想，如果我不惩罚分裂，那么最终结果是A-B1...BN，这个结果也不优美，惩罚分裂或许是对的。
再进一步想，或许泛化也应该惩罚。过度的泛化会导致层次结构失去意义，而我们也很难界定过度泛化的界限。我们或许应该再加一个设计，当一个节点ali过多的目标结点时进行惩罚？


我们一个一个来看。
1. ali(u) 是 reward 的“地基”，这点我很认同。但我不论选择哪种方法都无法避免这个问题。即使我用复杂的最straightforward的方法，在得到最终结构树时，也设计到对齐的问题。无论如何都有这个问题。
2. 我是一篇一篇的处理文章，并且每一篇文章实际上只能影响到自己这一部分子树。一篇文章没有分类到的类别，是一定固定不变的，所以我觉得对不变的部分比较reward并没有意义。并且我用的是iwsft训练，只会计算advantage。
3. 确实存在这个问题，那你能否优化我的reward计算？
4. 我觉得，这实际上就是一个全树的差分形式。就像我第2点说的，文章只能影响到自己经过的path，而我关注的是多条轨迹间reward的对比。
在理论上，我们完全可以按照全树的reward来说明，也就是把R_n改为T_n，继而证明greedy的有效性。而在实践层面，我们采用R_n进行对比就行。
5. 我觉得这已经算是很快的方法了。实际上我的主要成本在ali部分，是正比于len(I_n)的，其余的部分不涉及模型推理，可以很快出结果。或许你有其他的更高效的方法？

我觉得你这几点都是有道理的。
1. eward 写成显式势函数差分，这一步是合理的。也就是说，Φ(T)衡量T_n与T_g的相似度，直接将我的reward定义中的R_n替换成T_n。reward = Φ(T_t)−Φ(T_t−1)。
在这里，我更偏向于1-maxprob 惩罚。
2. 你将reward改成概率模式我认为是合理的，不过在实现时会遇到一些问题。
原本我的思路是，一个结点第一次进行对齐时，将T_g中所有的结点都作为候选集。后续这个结点对齐时，只把前一个对齐结果的兄弟结点和父节点作为候选集，这样能减小对齐模型的波动。
但你现在改成了概率形式，这种方法就行不通了，每一次对齐，都要将所有的结点作为候选集，这样会不会出现不稳定的现象？

回答问题，然后对整个流程进行一个系统的、详细的、符号化的总结。

我对步骤进行确定：
1-3. 没有问题。
4. 保留。
5. 不需要硬惩罚了。
6. 没有问题。
7. 需要动作成本，c_t = e * C(action)，e是一个超参数，C(action)当action是创建新类时为2，其他动作为1。
8. 没有问题。
然后你完整以下任务：
一、我需要你将现在的这个步骤写一个论文级别的步骤说明，写成一个.md文件。不用写实验中是如何做的，只需要写在理论上是如何设计的。
二、我需要你在上面生成的文件的最后，写一个数学上的证明。现在的reward已经是显式差分了，我需要你在数学上证明这个reward满足greedy算法的要求，证明我使用greedy是合理的。
三、再生成一个新的md文件，这里面只涉及实验的具体实现细节。要多关注细节，条理清晰。


可是在我们的设计中，由于无法合并、分裂结点，那也就不存在所谓的更好的改进空间吧。
我们先忽略cost，当处理一篇文章时，分两种情况，第一种是将文章最终分到现有类别中，第二种是将文章分类到一个新生成的类别中。我们假设当前的结构树是完全正确的、是T_g的一部分。T_n和T_g结点正确的一一对应，
考虑第一种情况，由于没有生成新的类别，因此这篇文章对结构树的框架没有任何影响，只会影响到path上结点的summary。那么最优的轨迹就是将文章分类到正确的类别中、并且在更新summary，结合文章内容summary趋向于T_g结点的title。
实际上不存在别的更好的轨迹。如果分类时有一步分错，那么分错的类别summary就会更新，包含文章的内容，进一步在对齐时会与T_g中正确类的对齐概率上升，进一步在这个节点关系的Score中降低。
如果在summary更新阶段，结点summary没有正确覆盖文章范围、没有与正确title贴近，同样的，对齐时概率就会降低，Score降低。
考虑第二种情况，由于生成了新类别，那么只要当这个新类别生成在与T_g完全一样的地方时，reward才会最大，在summary更新阶段与第一种情况类似。
也就是说，我们采取了reward最大的轨迹，实际上就能得到一个完全正确的、是T_g的一部分的T_n，而这个T_n会在下一步完美的符合我们的假设，继续生成下一个完全正确的T_n，到最终直接生成T_g。整个过程类似于数学归纳法。
而你说的给未来留更大发展空间这件事是无法实现的，当你在结构上留足空间以后，这个结构上与T_g的异构在未来不可能被逆转，因为我们的操作只有分类、生成新类和更新summary。如果是在summary上留足空间，那他最好、最理想的情况也只是与我们的greedy情况相同。毕竟我们可以通过每次去reward最大的轨迹的方法完全达到最终的满分。
你觉得我这样的想法有什么问题吗？

可是我现在是在做理论推导。我知道我的判别器不完美、对齐模型有偏差等等，当然最直接的还有我可能都无法采样到最优轨迹，但这些都是实践层面上的问题。
我们目前在进行的是设计reward、理论证明greedy的合理性，那就不应该考虑实践层面上的偏差吧。在理论上，“当前 reward 最大的轨迹”对应了“与 T_g 局部完全一致”的动作，至于这个轨迹能不能被采样到、对齐会不会出错，这不是我在设计理论时该考虑的问题吧。
包括你说的“为了当前文章让 summary 更贴近某个 title”的更新，可能让这个节点对未来另一批文章变得更不合适，那在后续处理文章时，最优的轨迹就是适应了这样的summary，在正确轨迹的指引下做出了正确的判断，这样的reward就是最高的吧。


我现在不去证明“reward更大则最终等分更大”这件事了，这件事确实不成立。我想证明的是，greedy的方法能够达到全局最优。也就是，每一步都严格采取最大reward的轨迹，则必然能够达到最终状态中的最优。

我发现我们之间的分歧在于summary更新阶段。我的想法一直是这样的。即使summary允许更新，即使当前summary写的有错、在后续能改正，那又怎么样呢？
我不需要证明到达最优只有greedy这一条路，我想证明的是，greedy这条路能到达最优。即使有多条路也无所谓，greedy的路只要是其中之一就行，我去证明“可达性”。
我证明了可达性，就可以说greedy算法没有降低整个流程能达到的上限。


你说的summary错误、后续即使修正了也会影响修正之前的分类。这句话隐含了一个前提，前提是就是在我们目前的reward设计中，summary的错误没有被惩罚。
当summary的错误没有被惩罚时，才会出现我采用reward最高的轨迹，但是summary错误这样的情况。而实际上，我们现在的势函数第一项是结点关系在对齐分布上的期望。而在这一项中，只有在T_n结点能够完全确定的对齐到一个T_g上的正确结点时，这个期望才会最大。
换言之，任何在对齐上的“不自信”都会降低得分。那么进一步的，当summary出现错误时，这个结点必然无法完全“自信”的对齐到正确目标。如果真的能完全“自信”的对齐到正确目标了，那说明这个summary本身就很清晰、正确，那也就不是错误了。


这是我的理解，看看我说的对不对。
对于第一条路径，我将现在的势函数改写成一个集合函数分数f(S)。只要我能证明f(S)具有子模型和单调性，那么我就能让greedy有近似比保证，reviewer也就能认可。
你说的方案A，我觉得很难满足单调性。在我看来，现在的势函数设计和方案A的f(S)差不多，但就像你说的，一个文章加入以后，可能会把一个summary写坏掉，降低这个结点对齐到正确结点的概率，降低f(S)。在我看来这虽然很像覆盖，但实际上不一样。
方案B、C的问题大致类似，我很难想到证明单调性的方法。
在我看来，f(S)无论从什么角度，都是衡量当前结构树的质量的，而这个结构树的质量很难单调，如果只有结构没有summary，那是单调的，但是永远有“summary写坏”这个问题。
到这里，我的理解对吗？
接下来就是你说的，要严格证明子模性会比较困难，那“近似子模/经验子模性”的路线又是什么？

我反而觉得，证明子模性并不困难。即使把现在的势函数的第一项直接作为f(S)，我觉得这就具有子模性了。势函数第一项作为f(S)，那么reward就是边际收益，“批量加入的一组元素的总增益”与“逐个加入边际增益之和”应该严格相等。
而且处理的文章越多，结构树越完整、包含的信息就越多，那么加入一篇新文章时，生成新类、需要调整的summary概率就越小，这也算是子模性了吧。
而看你写的，好像子模性是更重要的。你写的三点中，都是在近似子模，第三点中，不单调的问题你好像也融入到了子模性的问题中。难道说只要有子模性，单调性就没那么重要了？


我觉得应该选择能够证明单调子模的方法，也就是“目标树关系”。
但问题在于，我把ground set 选成“目标树上的关系集合”，把f(S)定义成覆盖到的关系权重之和，这真的是单调的吗？如果一个summary改变了，进而改变了对齐关系，那原本覆盖的关系也会失去啊。
我可不可以用这种方式来近似单调，也就是，当一个summary被修改时，我们需要进行重新对齐，而对齐的候选集选择原来对齐结点的兄弟节点以及父节点，这样即使对齐关系改变了，改变的也很小。以此近似单调？
总之，我现在更关系的是理论上的优美，为此可以修改之前的设计流程。


在这里，我有一个问题。我看懂了你的两个方案，我也更倾向于方案1，只不过f是“累计覆盖证据”的代理目标，Φ是优化目标，那如果这两个完全没有关系的话，那是不是有问题？
这二者完全没有关系，那就变成了我挑了一个f来证明可以greedy，但是用一个完全不相关的优化目标，这看起来没什么道理。
也就是说，我需要把二者联系起来，或者就像你说的，我们的 greedy 实际在最大化某种“覆盖证据”的边际增益。但如何说明这件事？最大化Φ为什么是最大化f的边际收益呢？

先说第二种，这我无法证明“有界扰动”。f是历史上的最好覆盖情况，Φ是当前覆盖情况，我要如何证明summary坏掉导致的破坏力有下届呢？即使证明了，Φ也可能是多次破坏叠加的结果，也没法证明最终结论。
我觉得第三种是一个很无奈的办法，我想到最后没的选了再采用这种方法。
那么就是第一种方法了。我觉得这样真的没什么问题。我直接令Φ中非常显式的包含f，写成你说的这样的格式，用f来衡量当前结构的一致性。
f是历史上最好的覆盖情况，这可能与“衡量当前结构树”的目标看上去不太一致。
那我采用reward = Φ(t) - Φ(t-1)的话，这个含义就是处理这篇文章后有没有对历史最优进行了改进。
这样的确满足了greedy的要求，但reward也就只能是非负了，好像对错误的行动也没有惩罚，这带来了一系列新的问题。


其实我没理解这两个有什么区别。第一个，我们要让Φ包含f所衡量的核心因素。而在二个，f是Φ的单调上包络，Φ与f的形式有很高的相似度，这不也是Φ包含了f的核心因素吗

我觉得应该选2。那么接下来就是g(r)的定义问题了。
首先，先帮我找一些现有的论文，采用了和方案二类似的方法的，最好能是经典的、被认可的论文。
然后我们来讨论g(r)的定义。g(r)表示的是在目标树上关系r被覆盖的概率。你觉得要如何定义？



现在我想明白了这个问题。我们犯了一个很大的错误。我们本质上是要设计一个reward，度量当前结构树和目标结构树之间的差异，差异越小得分越高。
而当前，我们对于greedy算法有效性证明不充分的本质原因，我们是从结点的角度来评判两个树的差异的，因此我们需要将树上的每个结点与目标树上的结点进行对齐，而这种对齐带来了状态的不稳定、reward的不单调等等问题。
那我们为什么不从文章的角度来度量树的差异呢？在两个树中，文章本身是毫无区别的，根本不需要额外的对齐。也就是说，我们要重新设计一个reward，当前结构树上文章的分类与目标结构树的差异。
从直观上来讲，这样的reward有很多优美的性质：
1. 每篇文章只处理一次，不会回头重新处理，因此每篇文章都一定存在一种处理方式是直通gold的、最优的，我们设计的reward只要给这种处理方式最高的reward，那么我们的局部最优就等价于全局最优，这是greedy里最强的性质。
2. 我们不再关注各个结点的summary，那只不过是一个结点的特征，我们从文章的角度去计算reward，避免了对齐模型的不稳定和对reward带来的波动。
3. 我们抛弃对齐模型，成本也大大下降。
你觉得我的这种想法对吗？


你说的这个前提就是我们要满足的。也就是，对一篇文章的处理是不回溯的。现在的处理流程中还包含一点点回溯的操作，我确定的说要把这一点点删掉。
而且，现在的操作空间中是没有合并和拆分的。虽然可以创建节点，但创建操作不会影响后续文章的上界，原因在于，对一篇文章最好的操作实际上就是目标结构树上文章的path，而后续的文章相当于在目标结构树的子树上进行处理，不可能降低得分上界。
而你写的s(x,T)是不对的，在这里你还是需要将两个结构树的结点进行匹配，不然不可能判断v是否等于g。我想的是完全从文章的角度。比如根据文章和哪些文章分在一类、和哪些不在一类、文章所处的层级等等这些信息，来计算reward，这才是完全不依赖结点。
你觉得我说的有问题吗？如果没什么大问题，下一步我想和你讨论处理流程的问题。现在的处理流程包含了一点点回溯，我们需要一个更清晰的文章处理流程。


是这样，我觉得当前的文章分类方式有一点问题，因此我不想在现有的基础上直接修改，而是只是使用“自上而下分类”+“自下而上更新”的思路。我用一个例子来说明比较清楚。
文章用d来表示，其他字母表示结点，比如gold结构树为：
Root
  A
    a1 - (d1,d2)
    a2 - (d3)
  B - (d4)

现在我们处理文章的顺序是3,4,1,2(随机的)
那处理流程大概就应该是
d3:
Root
  a2 - (d3)
d4:
Root
  a2 - (d3)
  B - (d4)
d1:
Root
  A
    a1 - (d1)
    a2 - (d3)
  B - (d4)
d2:
Root
  A
    a1 - (d1,d2)
    a2 - (d3)
  B - (d4)
我觉得这应该是正确的流程。这个流程与现在的区别在于：
1. 文章和最终的叶子结点绑定，比如一开始d3就被分到a2，然后d3就会跟着a2移动，这样避免了文章的回溯操作。
2. 现在这个流程可以直接生成中间结点。比如在处理d1时，模型判断d1属于a1，而a1应该与a2并列，那么这个步骤就可以在a1a2上面生成一个结点。
3. 这个流程不会生成连续的孤立的子节点。比如d3，没有直接生成A-a1，而是只生成了a1。而A是后续得到的。

我大概知道这个流程的过程，但是我不知道如何系统性的描述这个过程。
它大概就是分类模型自上而下的分类，分类到已有结点或者生成了新节点以后停止。我现在并不清晰的是按照什么规则生成中间的结点。
你能理解我说的流程吗？你觉得我的流程有什么问题吗？生成中间结点的规则你有想法吗？


先说你提到的主要问题：
第一点。你说我要允许“叶子拆分”，但我觉得这不对。如果a2需要拆分，那么属于a2的文章就需要重新判断它们属于拆分的哪一类，这相当于回溯操作。实际上我也没有允许两个节点的合并，我只是允许生成一个父类。错误固化就固化了吧，我感觉这是我整个设计中不可避免的东西。如果运行拆分和合并，那所有工作都需要重做，而且还需要解决循环的问题。
第三点。我希望用现有的系统框架。也就是有一个分类模型，负责进行分类和是否需要新类的判断；一个总结模型，负责生成、更新结点的summary。每个结点还是由summary来表示其范围。
第四点。和上面那一点一样，我想用现有的系统框架，直接让分类系统来判断分到哪一类、是否需要新类。

然后是你说的生成中间节点的规则，我觉得你第二点写的很好，也就是当我新建一个叶子结点时，判断与当前父节点下某一个孩子是否应该先归为一个更细的共同父类。
但我不想用相似度这种方法。就像我上面说的，我更希望使用大模型的生成和泛化能力来做这件事，因此我的想法是将这个判断功能融入总结系统。也就是，在总结模型生成新类时，写完新类summary以后，进一步判断是否应与当前的一个类有共同父类。
你觉得我这样操作可以吗？


那就这样做。给总结模型输入文档、当前结点summary和子节点summary，模型需要根据文档内容生成一个新结点summary，并判断这个结点是否与其他结点建成父类。
输入全部的子结点就行。我的结构树比较小，一个结点下最多也就十个左右的子结点，且每个结点的summary长度较短，不会出现上下文爆炸的情况
那我们现在就这样决定，现在需要你写一个说明md文件，先说明一篇文章处理步骤，用符号化的形式进行详细说明。在之后我们还会在这个文件中加入reward、证明等内容。



接下来我们讨论一下reward的问题。我们现在的这个处理流程中，文章不会回溯，因此reward需要满足以下条件。
1. reward度量的是当前结构树和目标结构树的差异，差异越小reward越大。
2. reward是从文章的角度计算，不涉及当前结构树和目标结构树的结点对比。
3. reward满足greedy的最优原则，局部最优即全局最优。也就是，每篇文章只处理一次，而reward最大的处理方式就是这篇文章最佳的处理方式。
4. reward不能忽略层级信息。虽然不从结点的角度计算reward，但是层数的正确性（或者说叫分类粒度）必须影响reward，它的权重可以较小。
5. reward最好是加和的形式，这样我在比较时只需要关注文章影响到的部分，其他作为常数项被消掉。
对此你有什么想法？如果有的话，请详细说明你的方法和计算过程、原理等等

你说的第5点，你不许啊哟关注我在操作的时候如何进行结构编辑。我需要的是一个证明，证明在我当前的文章处理步骤和reward设计下，单步reward最优等同于全局reward最优。
就好像迪杰斯特拉算法一样，证明我当前选择reward最大的处理方式等同于取全局最优。
或者你证明，在处理一篇文章时，当前reward更大一定等同于最终reward更大。
你不需要关心我们在实践中能否真的找到reward最大的处理方式，你只需要理论证明即可。

我有以下问题：
1. 条件S和定理之间是否是充分必要的关系？
2. 我是否可以退一步，证明（1）给定文章处理顺序和目标结构树，则有且仅有唯一一个轨迹达到目标结构树。（2）在唯一轨迹中，每一篇文章的处理正好对应reward最大的操作。
我觉得这件事是可以证明的，首先（1）我觉得是没问题的，因为我们的操作不允许文章回溯，因此只要按照我们的操作能够重建目标结构树，那么在给定文章顺序的情况下，每篇文章必然有唯一正确处理步骤。
而这个正确的处理步骤，我们应该设计reward，让这个步骤的reward最大。

我问这两个问题，是因为我自己是这样想的：
（1）如果将两篇文章错误的分到一个类中，那么此时的reward一定不是最大的。因此，从文章的角度来看，得分最高一定对应着所有文章分类正确。
（2）在所有文章分类正确的情况下，考虑结构。我们只有“将一些子节点分到一个新的父类下”这一个操作会影响一篇文章对于reward的贡献，这个操作我们叫他“生成父类”。
  当所有文章被分类正确时，如果“生成父类”错误的改变了一个类别的层级，那么reward必然不是最优。如果一个类别的层级小于正确层级，那么加大这个类层级会增大reward，而层级又无法减小。
因此，对每一篇文章，reward最大的操作就是：将当前文章正确的分类，在其经过的路径上，将所有层次不正确的类别尽可能的采用“生成父类”的操作加大层级，对层次正确的类别保持不变。
那这不就是我们在处理这篇文章时唯一正确的操作吗？
因此，从空树开始，每次都采取reward最大的处理方式，就是每一步都采取了唯一正确的处理方式。


2.1 你说的对，唯一轨迹确实不存在。我应该证明的是，greedy的策略对应了一个到达目标结构树的轨迹。其实是有重挂的早晚会区分轨迹的，而greedy实际上只是采取了尽早重挂的策略。
  而你说的硬约束其实不存在。如果在“孩子没凑齐”时就提前 InsertParent，那么后续文章会直接进入这个父类，然后直接在这个父类下生成子类，不需要等子类都凑齐。
  我可以在InsertParent增加约束。比如我在生成新类后判断，新类是否应该与其他一个（只有一个）子类收拢在一起。这样轨迹就唯一了，因为没有早插/晚插的争议，而是只能收拢一次。
2.2 我觉得就是你说的可能B啊，我的确允许后续操作改变旧文章之间关系，但是我上面已经说明了，我可以用数学归纳法，我每一步都采用reward最大策略，那么我一定得到一个“每篇文章都分类正确”且“所有类别的层级小于等于正确层级”的结构树，
  在这个结构树上，我继续采取reward最大的处理方式，那就是把这篇文章也分类正确，并且如果叶子结点层级小于正确层级，就重挂。
  每个叶子结点都至少有一篇文章，且一个中间结点下至少有两个子节点。那么，一个深度为n的树下面至少有2^n篇文章，2^n > n，那么2^n篇文章经过这个结点，就至少能把这个结点的深度推到正确的层次。
  也就是说，greedy保证了所有文章分类的正确性。每个节点下至少2^n篇文章保证了这个每个结点的深度达标。那么所有结点的深度达标、所有文章分类正确的结构树，那不就是目标结构树吗？


你说的第三点有问题。我从不关心“叶子名称与 gold 如何对应”，我只关系文章直接的分类关系，我是完全用“文章视角”定义正确性。
而你说的那个例子中三篇文章abc，目标希望ab更近、c更远，那只有一棵树能满足要求：
Root
  A
    a1 - (a)
    a2 - (b)
  B - (c)
就这一棵树，至于ab所属的共同祖先是A还是B我不关心，怎么会生成不同的树来满足要求呢？

你说的这两棵树，一个是ab在同一类中，另一个不在，这在目标结构树中是很明确的，而这两颗树的reward相同，说明我们的reward设计有问题，没有区分这两种情况。
现在我们先不管reward具体是什么了，我们先假设，现在有一个reward，在不对齐结点的情况下能够非常明确、敏感的识别当前结构树和目标结构树的不同。
在这样的reward下，是不是就可以按照我之前的归纳法证明了？greedy保证所有文章分类正确，每个结点下至少有n+1篇文章（之前是我算错了），保证了树至少能到n的层级。这样就和目标结构树完全一致了。


可我对“分类正确”的定义就是很严格啊，也就是一篇文章在正确的结点与其他文章分叉，最终和正确的文章一起分类在一个叶子节点下。换句话说，一个文章“分类正确”当且仅当这篇文章与其他所有文章的关系正确。
而两篇文章的关系包含了分叉点以及各自和分叉点的距离。

我从没有像用Dijkstra 式结论，我也问你了，单步最大 ⇒ 最终最优这是一个充分不必要的条件，因此我提出了自己的数学归纳法证明啊。
在这种强关系下，greedy保证了强关系下文章的分类正确，每个结点下n+1篇文章保证了每个结点的深度都达到目标，这不就行了吗？
我证明了，每步都严格采取reward最大的处理方式，最终得到的就是全局最优


你说的对，首先第一点，引理A我自己想明白了，但是没有说清楚。你说的第二点，也有道理。
我重新组织了想法，我现在从头到尾仔细的说明我的过程。
1. 文章之间的关系是指二者的分叉结点深度（LCA），同一类的文章之间关系视为-1。文章分类正确指它与当前其他文章的关系都正确。
2. 势函数Φ(T) = sum f(LCA(i,j) - LCA_gold(i,j))，函数f的细节后面再说，但思路是当LCA小于目标时，增大LCA则增大势函数；当LCA大于目标LCA，直接给一个超级大的惩罚。
3. 重挂操作将重挂的生成的父节点下所有文章之间的LCA加1，与其他文章的LCA不变。
4. 由2、3可知，如果一个子节点A下存在文章之间的LCA与LCA_gold相同，那么重挂这个子节点会受到很大的惩罚，因此只有当一个子节点下所有文章之间的LCA都小于LCA_gold，重挂操作才有正收益。
5. 数学归纳法证明，当且仅当所有文章之间的关系都正确，则树的结构与目标一致。将一类文章与其他文章的LCA从大到小排序，依次构建树，构建方式唯一，因此树唯一。这个方法或许不是归纳法，我没有详细写过，但意思是这个意思。



其实在现在的设计中，历史文章的间的关系还是会被改变。重挂操作将u和v挂在一个新的父节点下，那么u下的文章之间的LCA就是u，而u的深度加1了，因此无论如何都会改变。
我仔细的思考了这个问题。其实问题不在于我们设计的问题，而是我们的大方向出错了。我们现在是在用文章的角度来度量两个树之间的关系，而树是包含结构的，无论我们如何度量这个结构，结点对齐也好LCA也好，只要我们对结构进行了调整，历史得分就必然改变。但是我们的整个操作流程中又必然包含对结构的调整。
因此我觉得有以下两个解决方法：
1. 仍然在reward中包含结构，也就是LCA，但是我们换一种greedy的证明方法。我刚才自己列举了我能想到的所有情况，在每一步采取正确操作的情况下，都能重构目标结构树。因此应该是我们的证明的方法有问题。
我们现在的证明建立在“历史文章关系不变”的前提上。那我们能不能巧妙的重新设计reward和关系的定义，然后证明这样两件事：
（1）在reward取最大的情况下，所有文章间的关系严格单调增，也就是不会变差。这一点应该是很好证明的，因为我们不对历史文章直接操作，它们只有可能因为依附的叶子结点变深而变深，我们在关系的定义中包含这种情况即可。
（2）用我们的操作流程，所有合理的可能的树结构都可达。

2. 我们在先不管结构了，只用两篇文章是否在同一类中作为关系，然后用关系的正确与否来计算势函数。这样我们就完全抛弃了结构上的一致性。然后，当一个结点下的子节点数量超过目标结构树这一层子结点个数的最大值，就给一个惩罚。
我们通过这种方式来“软约束”结构。这样，得到的最终结果是文章分类正确，但是结构上会有差异。这样我也能接受。如果是这样，能不能严格的证明greedy的有效性？

我们先来讨论方案1.
我这样做可以吗？让势函数为，当前LCA小于等于目标，就用当前LCA；当LCA大于目标，给一个-inf的惩罚。用这种方式来保证greedy路径中的InsertParent操作不会推错。
但是这样做这个势函数就不是直观的单调不下降的了。但是我们可以证明在greedy路径中这个势函数是单调不下降的。如果所有的LCA都达标，那么等同于目标结构树。如果有的LCA不达标，那么其中一定包含需要被推的叶子节点，也就一定存在reward>0的操作。greedy永远不会选择reward小于0的操作。
这样可以吗？


我把你写的文件按照我自己的方式进行了总结，在 符号化说明与greedy有效性说明.md 中。
在这里面，我进行了一些改动：
1. 我把L改为了R，在两个文章输入同一类时让R为-1，这样当文章被错误分类时会被惩罚，不然会忽略这一错误。
2. InsertParentPath可行性不止会让夸ab的文章关系加1，而是除了同一叶子结点内的R，其余v下面的文章之间都加1。不过这并不影响后面的说明。
有一些我没有写：
1. 你说的2.4“纯度”类假设，这个假设我觉得可以用和4.1类似的方法证明，你试试。
2. 在流程中，我们只会在创建新类时有可能执行一次InsertParentPath，而不是反复的使用。你需要证明最优的可达性。
直接在 符号化说明与greedy有效性说明.md 中修改即可


1. 首先，.md文件写公式的起始符号是“$$”，而不是“[”和“]”，行内也不是“（）”。
2. 我觉得还是应该在势函数中加入-inf的惩罚项，因为这不止能够惩罚“推错”，也能惩罚“分类错误”。当分类错误时，目标R为-1，实际R大于等于0，这中情况也会被惩罚。
  这样写势函数也满足了引理5.2的假设H1，因为会被惩罚，所以greedy策略下不会分类错误。H2假设不也是吗，在greedy策略下，不会有InsertParentPath无法通过TC。
修改文件


1. 5.4的证明不要说和之前一样，直接写出来。
2. 第六节缺少一步，当文章关系矩阵与目标完全一致时，两个树的结构一致。这个很好证明的。
3. 引理5.3，你说现在的证明还不是完全严格，缺失选择y的策略。那我觉得使用6.1的InsertParentPath策略就能最优，你试试证明这个策略一定是一个正增益动作，那么这个证明是不是就严格了。



我明白了，这里是我的想法错了。我不用证明greedy的方法是唯一正确的，我只需要证明我采用greedy的方法能达到全局最优的上界就行。
也就是，我需要的是更详细的第六章可达性的证明。命题6.1的证明并不完善。证明只给出了一个策略，没有说明这个策略对应的reward是最大的，也没有证明这个策略如何达到最优。


那按照我的理解，C_t中所有的结点u^*，它是T^*中的结点，它对齐了当前结构树中的一个结点，这个节点下所有的文章分类正确，深度与u^*一致。
那你在证明的过程中，归纳步的第三点，如果触发一次InsertParentPath，为什么你能把u^*加入C_t？u^*还不满足深度的要求吧。
不止于此，我也没明白不变量第三点的作用是什么。
整个过程有一些抽象，你能举个例子说明吗？


我大概明白了一些你的意思，但还不是完全清楚。而且你的这个例子有些简单。
我举一个例子，你来说明整个过程，写成逐步状态表。
目标结构树：
Root
 A
  a1 
    a11 - d1,d2
    a12 - d3
  a2 - d4
 B
  b1 - d5
  b2 - d6
文章处理顺序： 1,3,4,5,6,2


有两个问题：
1. 我觉得你现在的证明写的并不清晰。首先，你定义了三个不变量，并且用归纳法证明了这三个性质的确不变。但是这三个性质不变推出最后结构树等于目标结构树这一步说的很不明白。
  的确，这三个不变量成立，那为什么最后这个结构树就变成了目标结构树？也就是你“终止”的证明很差。
  我大概看懂了你想表达的意思，而我是这样理解的。我们现在想要证明greedy的策略能够取得全局最优，而我们本质上是规定了一个策略π作为中间值。我们要证明：
  （1）greedy的每一步等同于π。
  （2）π这个策略能达到全局最优。
  那我们更清晰的表述方式是：
  （1）明确的写出π。
  （2）证明我们的每一步reward最大等同于π。
  （3）证明π的策略能够最终达到全局最优。
2. 在你的例子说明中，我觉得在处理d3时有问题。这一步不应该归拢生成A，而是应该归拢生成a1。因为我们规定InsertParentOnce是一个已有类加生成的新类进行归拢，
  如果你在这一步生成了A，那么在后续处理d4时，却生成a1时，就违背了我们的设定。
  正确的流程应该是，处理d3时归拢出a1，然后在处理d4时，再把L2和a1归拢出A。


这一次你的证明写的很好，我把说明总结、精简了一下，请你作为参考指导后面的行动。接下来我们来讨论实验的问题。
我整个的设计思路是，因为我的任务是依次处理文章，重构目标结构树，因此这是一个很直观的强化学习的设定。
但是难点在于每次处理文章都有很多action，文章也多，会导致轨迹非常长、credit assignment难以解决。
因此我使用greedy的方法，我们刚才也证明了，greedy的方法等价于全局最优。这样我们把问题转变为根据一篇文章的处理轨迹来进行强化学习。
这里我有两个问题：
1. 我们整个处理的流程中一直忽略了一个步骤，就是summary更新这一步。也就是说，在整个流程中，是分类模型进行分类，总结模型生成新类或者归拢，然后再自下而上更新summary。
  但是整个reward中是无法反映更新summary这一步的效果的。这个效果一部分要在后续的文章分类中才能体现，一部分是看是否满足规则，也就是不与兄弟结点重复、不超出父类范围。
  也就是说，更新这一步无论如何采样，都不影响reward，那我们如何训练这一功能？
2.这里我就有了一个想法，在证明时，我们已经有了一个很明确的策略π，它依赖于目标结构树的oracle，那我们能不能不管这一篇文章的处理轨迹了。我们不去采样、不去强化学习，而是直接用这个策略来指导模型？
  也就是说，处理一篇文章时，我们能否直接根据策略和目标结构树得到一个正确的路径，然后直接用正确的路径指导模型？
  这样我们能抛开强化学习的复杂和不稳定，目标也明确。不过这也会碰到第一点问题，如何指导summary的更新呢？


1. 我准备采用从策略π出发结合oracle直接获取训练数据的方法，因此需要你写一个新的训练流程，不再使用现在的采样轨迹、计算reward、iwsft训练的方法，而是直接获取数，然后训练。不要删掉现有的方法，新写一个。
2. 在updater中新加一个模式。现在是通过模型来生成summary、更新summary。新模式不需要这么麻烦了，而是直接统计词频作为原本的summary。生成summary就是统计词频，更新summary就是更新词频，归拢操作就将两个summary合并。
3. 在分类系统中加一个功能，现在是让模型判断将文章分到哪些类、是否需要生成新类，现在加一个，在判断需要生成新类时，继续判断是否需要将新类与现有的哪一类归拢。不是分两次输入，而是一次判断。
4. 现在的文章处理流程需要修改。现在还是从上而下分类、自下而上更新的处理方式，而现在的处理方式就像有效性说明文件中第二章那样，从上而下分类、生成新类，采用InsertParentPath、调整结构，然后自下而上更新summary。
5. 生成数据的方法为，先按照我们的设计实现策略π。在生成数据的时候，你要同步保存当前结构树节点与目标结构树的映射，然后以此实现策略π。对所有train topic，打乱文章顺序，然后真正的模拟结构树的重构过程。读入一篇文章，根据策略π和C_t真正的处理一篇文章，在这个过程中吗，保存分类模型应有的输入和正确的输出，
  然后调用updater的词频模式，直接按照流程处理文章。也就是说，采用完整的文章处理流程，只不过不调用分类模型，而是直接用策略π处理，然后记录正确的输入输出。
6. 最后调用生成的数据，采用sft的方法训练模型。


回答你的问题：
关于2，你说的对，要明确summary的外部表示格式，你说的这个格式就可以。并且要固定top_k，以免输出过多的词。还要去掉无用的词，比如语气词、助词等等。
关于3，这里你要修改分类模型的prompt，让模型多输出一次判断，你这个输出的格式就可以。不过不需要要求模型的判断满足TC，而是我们在生成数据时，策略π已经被证明满足TC了，然后训练模型就行。
关于4，不是。首先，InsertParentPath 发生在 bottom-up 之前是对的，但不是必须归拢。需要让分类模型判断是否需要与一个现有类别归拢。整个流程请你去看 符号化说明与greedy有效性说明.md的第二章。
关于5，你数哦对是正确的，需要在生成数据的时候维护映射。


我仔细看了代码，觉得我之前说的有些问题。我之前说，当一篇文章有多个路径时，分别处理多次。这样并不好，原因一是第二次处理时不知道第一次选了什么，会有冲突。原因二是处理完以后的合并很困难。
现在这样处理我觉得会更好一些。一次性处理多个路径。在prepare_dataset_oracle.py的decide_top_down_child时，选出所有满足要求的子类，然后都分别进行递归处理。
只有一点需要注意。当需要一次性创建多个类时，NEW为Yes，而归拢的类取按照第一个新类路径。其他的就先忽略掉。


有以下几个问题：
1. 现在的updater加了一个词袋模式BOW，这个模式的summary就不需要使用之前的格式了，也就是不必再分解释和范围，直接给出词频统计就行。
2. 在把当前结构树的结点与gold的结点对齐时出了错误，对齐时如果有多个结点符合要求，没有采用最深的原则。比如一篇文章的path是Root-A-B-C，那么假设Root下没有结点，那么处理这个文章时，gold上的ABC都符合新节点的对齐要求，那么应该在这些有继承关系的节点中选择生成Root-C。而你现在生成是Root-A。

我重新说一下吧，可能是我没有说清楚。
在对齐结点时，不需要两个结点的深度一直。因为一开始创建节点时，它必然处于浅的位置，是后续的归拢操作一步步推到指定深度的。因此，在对齐结点时，要求的就是结点下属的文章正确、当前深度小于等于gold的深度。
那么在路径Root-A-B-C中，gold的A、B、C都满足上述条件，那么我们应该选择最贴近文章的C来对其。C下属的文章范围最小，更贴近当前需要对其的新建的结点。


有两个问题：
1. 在处理了几篇文章以后，root的target_label没有指向gold的根节点，不知道具体原因。
2. 在判断分类时出错。按上面的例子来说，已经处理了一个文章，path是Root-A-B-C，然后生成了Root-C。接下来来了一篇文章的path是Root-A-D，这种时候应该创建新类，变成Root-C、D，然后归拢得到Root-A-C、D。但是现在的程序直接将新文章分类到C中了。

三个问题：
1. 在现在的逻辑中，要么选择现有的分类，要么创建新的结点。但实际上，二者可以同时发生。比如一篇文章有两个path，一个已经在现有类中，另一个需要新建，那么就是一个继续递归分类，一个创建新类。分类模型输出也是有选择出的index也有NEW。
2. 不知为何，生成的path最多取到深度3。比如一篇文章的path是Root-A-B-C，对应的gold结点却是Root-A-B。我已经把config文件中的max_depth改为5了还是不行。是程序把参数写死了？还是gold树被限制了深度？
3. 归拢时，输出的index有可能错误。比如现在Root下有7个类，0-6,6号是新建的类，判断归拢时，新建的6号应该和5号归拢出一个新类，这时，输出应该是5号。但是我第一次运行，这里输出的是4号，第二次运行，这里又是正确的5号。查查看原因。


归拢时，新生成的父节点对应关系有问题。比如有两个文章的path是Root-A-B和Root-A-C，当前是Root-B，然后新文章来时，得到Root-B、C，此时将BC归拢，应该得到Root-A-B、C。但是实际得到的是Root-C-B、C。也就是，新生成的结点没有正确的对应。

看来我说的还是不够清楚，你的理解有一些错误。
不是“新父节点应该对应gold树depth=1的节点”，这回到了之前的错误逻辑，也就是当前节点必须对应相同深度的gold结点，这是不对的。
实际上，在我说的例子中，正确结构是Root-A-B、C，在对应时，归拢给出的新节点下有两篇文章，包含这两篇文章的最小的gold结点就是A，因此才会对应A。而不是按深度来直接对应。

归拢时出错。当前有7个类，0-6号是新建的类，判断归拢时，新建的6号应该和5号归拢出一个新类，这时，归拢结束后，只剩下了5个类，莫名其妙的少了一个。

分类时出错。假设当前的结构树为Root-B，文章的path为Root-A-B-C，在第一次分类时，chosen_indices能正确判断出应该分类到B，但是后续会被判定需要创建新类。
这肯定不对。文章的path是Root-A-B-C，那么就应该直接分类到B中即可。

现在错误更多了。现在出现了父类是Root-A-B而它的一个子类是Root-B的错误。

你现在完全走偏了。每一次分类都有一个固定的处理流程。在被分类到新类时，将分类的结点作为父节点，重复这个固定的处理流程。
处理流程在 符号化说明与greedy有效性说明.md 的第二章说明。
只是把获得模型结果换成使用策略π来替代，然后记录正确的输入输出。
策略π在 符号化说明与greedy有效性说明.md 的5.2说明，唯一要补充的就是，当有多个目标结点满足文章覆盖的要求时，选择最深的。


文件中可能没说清楚，你现在实现的逻辑是有问题的。文件中描述了四个允许的操作，没有说清楚每个操作的触发时机。
处理一篇文章的流程是：
1. 把根节点设置为当前节点。
2. 把文章、当前节点、当前节点的子节点输入分类系统，判断把文章分类到哪些子节点下、是否需要创建新节点、如果创建新节点是否需要归拢。
3. 根据输出创建新节点、归拢，如果需要的话。
4. 对于输出的被分类到的节点（不包括新节点），把它们设置为当前节点，重复2-4步。
5. 从被分类到的叶子结点开始，自底向上更新结点summary。

与你现在的区别是，不会在叶子节点下继续创建新的叶子。如果一个结点是叶子结点，那么直接结束了。
生成数据时，把分类系统的输出替换成策略π的输出，记录所有的输入和输出对。

对于你在“需要修复”中指出的问题：所有的都生成啊，这个没有丢掉的选项。所有给策略π的输入和对应的正确输出，都要被记录。

你误解了我的意思。你在298行开始“选择最深的目标节点”，都是当前节点的子节点，肯定深度一样啊。你的策略π写的也很怪。
我们所有的深度、文章覆盖都是在正确的维护两个结点的映射关系。一篇文章的path是Root-A-B-C，当前结构树为Root-b-c，然后b对应B，c对应C，那还判断什么文章覆盖，直接把文章分类到b-c就行了啊。
所有的深度、文章覆盖都是在生成新类和归拢时找到映射关系使用的，所谓的“选择最深的目标节点”也是在这个时候起作用。也就是策略π涉及到这些，跟分类流程毫无关系。
比如一篇文章的path是Root-A-B，现在的结构树是Root-C，gold是Root-A-B、C，那么这时先生成新类，然后判断生成的新类对应C，得到Root-B、C，因为在找对应时，这篇文章的path是A-C，而C是最深的，所以选择C。
然后继续判断是否需要归拢，当前结构树为Root-B、C，那么就应该归拢出一个新节点。在找这个新节点的对应时，发现节点A覆盖B、C的文章且是最深的，因此这个节点对应A。就是这样。

你没理解我说的例子。
gold为Root-A-B、C，这里面BC是同层的、属于A的子节点。等价于：
Root
  A
    B
    C

问题：
1. 299行，为什么只选一个？应该允许多分类啊。一篇文章可能有多个path啊。
2. 策略π中，一篇文章的path为A-B-C，现在有一个节点，它对应的目标结点是A-B-D，为什么会把文章分到这一类之中呢？


你现在的判断还是错误的。首先，策略π你就应该写到策略π的代码里，不要在这里面直接写。
其次，298行开始出错，前缀一样有什么用？按照我上面的例子，一篇文章的path为A-B-C，现在有一个节点，它对应的目标结点是A-B-D，那应该新建一个类别啊？这俩不是一类的啊？
你应该比较的不是前缀，而是全部。如果当前path为A-B-C，那么A能接受，B-C能接受，B-D就不能接受。


你在更新summary的时候是不是写错了？新归拢得到的类有没有正确的统计词频？我发现了有的父类词频不是子类词频的和。


现在的代码我没发现什么问题，接下来我想和你讨论一些关系summary的问题。
我把summary由原来的解释+范围改成了词频，优点是简单、直接，它不需要模型生成、不需要训练、不可能出现超出父类范围等问题。这样即使我的策略π不涉及summary，也能处理和训练。
缺点也很明显：
1. 前期的波动很大。当一个类中的文章很少时，类的词频会被少量文章词频带偏。
2. 表达能力下降。词频无法直接表示范围。
有没有一些折中的方案？让我在保持它优势的同时尽量避免这些缺点？


Q1-a、Q2-b、Q3-a


像run_pipeline.py那样写一个可控制的流程，把生成数据、训练模型和推理三部分做成三步，让我可以自由的选择跳过哪些步骤。
检查推理的代码，现在的推理流程和之前的有区别了。整个流程和生成数据时是一样的，只是用模型来推理得到结果。现在只有一个模型，因此要用两个卡加载模型。


现在有一个问题。我现在的分类模型的任务是，把文章分到已有类别中或者判断需要新类，如果需要新类，则判断是否需要与一个现有的类归拢。
这里对于归拢操作的比例很难控制。当我在训练数据中设置归拢操作数据占比为0.4时，模型会过度归拢，生成深度100以上的树。当我设置占比为0.25时。模型又过度不归拢，根节点下50以上的子节点。
我觉得可以是以下原因：
1. 归拢操作只允许在创建新类时执行一次，这限制了归拢的时机，我们或许可以允许更多的归拢来解决问题。
2. 通过控制数据集比例的方式控制归拢操作的频率，这种方式还是太过于模糊，不够直接。
你觉得这个问题该怎么解决？我说的是否有道理？


先不要修改代码，还有问题没有讲明白。
1. 当前oracle数据集生成策略没有问题，如果要在分类模型的输入中加入结构信息，那也只需要在生成数据时相对应的加入结构信息就行，不需要修改策略和逻辑。
2. 增加了结构输入可能会缓解这个问题，但一些问题还是没有解决：
首先，数据集的比例依然会影响模型执行归拢策略的频率，这我只能通过多次训练来尝试最佳比例吗？
其次，我现在用的是bm25得分来表示一个类的特征，这是不是信息量不够？有没有可能是因为信息量不够导致模型无法明确的学会什么时候该归拢，导致归拢操作更多的受到数据比例的影响？


我又仔细的想了一下，我觉得出现这个归拢操作不稳定的根本原因在于，oracle策略虽然是能指向全局最优的最佳策略，但它是在一个新类被首次创建时就直接归拢。
而在这时，新类的特征完全被这一篇文章所引导，其展现出的信息很大概率不足以和归拢目标有足够的相似度。只有当两个类的文章都有了一些，两个类的特征有相似度时，在模型看来归拢才是合理的选择。
所以我准备采取这样的方法：
1. 修改分类模型的输入和prompt，输入增加每个子类的结构特征（子类个数和子树深度），然后在prompt中要求：一个类别下的子类不能过多，子树深度不能太深。子类多就要更大概率归拢，子树深就要更小概率归拢。
2. 取消归拢的限制。分类模型在每次分类都允许归拢。也就是说，模型将文章分类到已有类别A，NEW为False，此时也可以输出一个归拢类别B，将AB归拢出一个新的父类。
3. 修改生成数据的策略。现在生成数据的策略是直接根据策略π的指导来生成，现在改成这样：
（1）新生成类时，策略π给出这个类别对应的目标类别和归拢目标，但是先不触发归拢，而是将这个几个类置于“待归拢”状态，继续后续文章的处理。
（2）在以下条件满足时触发归拢操作：a. 当一篇文章分类时经过了一个“待归拢”状态的类; b. 这个类的特征与“待归拢”的类的特征相似度达到一定阈值或者当前子类的数量达到了一定阈值; c. 这篇文章是这个类别的最后一篇文章。
这样相当于模拟了更真实的分类场景，当这个类别的特征明显、子类过多和文章分类进行到后期时触发归拢，这样让模型学会什么时候归拢，也解除了归拢的限制。

你觉得这个方法有问题吗？可行吗？


1. 可以在分类模型的prompt中说的更明确一些。归拢会导致子类数量减少、树变深。当子类过多时，更多的归拢。深度太深时，减少归拢。
2. 数据流图有一点小问题。不是只在归拢时记录生成数据集，而是每一次分类操作都生成数据。


执行归拢以后，新的父类没有更新其summary

还有问题。你现在记录待归拢状态结点是两两配对，这是不对的，你应该记录的是多个结点组成的集合。
比如，目标结构树为：
Root
 A
  a1
  a2
  a3
 B
那么一开始，生成的结构树就有可能是：
Root
 a1
 a2
 a3
这样你记录的pending为(a1,a2),(a2,a3)
当你把(a1,a2)归拢后，a3就再也不可能被归拢了，因为他们已经不在同一层级了。
你应该记录的先是(a1,a2)，a3来以后，pending变为(a1,a2,a3)，然后把这个三类一起归拢。
也就是说，归拢不一定只能选一个。



1. 805行，你没有实现多个分类时的递归
2. 我发现了一个漏洞。如果文章被分类到了一个叶子结点，没有创建新类，那岂不是程序会继续运行，在叶子结点下继续创建节点？但实际上应该直接结束啊。


请你改进一下两个类计算特征相似度的方法。我们要忽略得分低的词语，只取输出在prompt中的前几个词。


我现在遇到了一个挺严重的问题。我把avg_similarity的阈值设为0，num_siblings的阈值设为无穷大，在这种情况下，最后根节点下有29个子节点，残留的panding记录有13个。
也就是说，这些“待归拢”类之间到最后也没有任何的相似度。它们可能在词义上有相似，但无法通过bm25+top_k这种方法检测到。
我现在有两条路：
1. 不再使用关键词作为类的特征描述，改回之前的summary版本。这样做的缺点在于，我不知道如何训练summary生成模型，我没有想到任何的能够指导它的信号。
  或许我可以固定这个模型，只训练分类模型，来适应这个固定的summary，但我不确定这样做的预期效果如何。
2. 继续使用现在的关键词作为类的特征，然后在判定归拢时机时，采用我之前说的，子类超阈值归拢、文章处理到一定程度也归拢。
你觉得我该怎么做？或者你有没有更好的方法？

我觉得这个方案是有问题的。我觉得残留的pending不只是单词不相同，可能用embedding的方式也无法通过。我找了一个残留的pending的关键词，你可以看一下：
'{"redrawing": 29.3, "treaties": 24.0, "penalties": 22.0, "germain": 20.0, "mandate": 20.0, "provisions": 19.0, "imposed": 18.0, "lausanne": 18.0, "mandates": 17.7, "dissolution": 17.0, "punitive": 17.0, "restrictions": 16.0, "trianon": 16.0, "vres": 16.0, "dismantled": 15.0, "prohibited": 14.0, "limitations": 13.0, "lavender": 12.0, "partitioning": 12.0, "heffernan": 11.0, "transcription": 10.0, "pugliese": 10.0, "twelve": 10.0, "conventions": 9.0, "palestinians": 9.0, "israeli": 9.0, "realignments": 8.8, "statehood": 8.0, "slovaks": 8.0, "1928": 8.0}'
'{"zombie": 54.0, "mode": 49.0, "zombies": 46.0, "pc": 42.4, "horde": 37.2, "xl": 34.0, "kamchatka": 32.0, "upgrade": 30.4, "vatican": 30.0, "snowbound": 30.0, "perks": 26.7, "paramount": 26.0, "undead": 22.0, "gb": 22.0, "os": 20.0, "amd": 16.0, "intel": 16.0, "radeon": 16.0, "cpu": 16.0, "ram": 14.0, "windows": 12.0, "unrated": 12.0, "zekes": 12.0, "monstrosities": 12.0, "flesh": 12.0, "unleash": 12.0, "gunslinger": 12.0, "slasher": 12.0, "fixer": 12.0, "dronemaster": 12.0}'
'{"copilot": 30.0, "365": 26.0, "microsoft": 24.0, "privacy": 19.0, "layered": 18.0, "malicious": 18.0, "prompt": 18.0, "injection": 18.0, "controls": 17.0, "compliance": 16.0, "tenant": 16.0, "lifecycle": 16.0, "vulnerabilities": 14.0, "operates": 14.0, "user": 14.0, "safeguards": 14.0, "assessments": 14.0, "containment": 14.0, "unauthorized": 14.0, "enforced": 14.0, "drohnen": 12.6, "directories": 12.0, "applies": 12.0, "penetration": 12.0, "enforces": 12.0, "leakage": 12.0, "classifiers": 12.0, "agentic": 12.0, "exfiltration": 12.0, "scoped": 12.0}'
'{"guilt": 25.0, "adolf": 19.0, "criminals": 18.0, "carriage": 18.0, "hitler": 17.0, "accept": 16.0, "442": 14.0, "doomed": 14.0, "disregarded": 14.0, "signees": 14.0, "mandates": 13.0, "shaken": 12.0, "fallout": 12.0, "761": 12.0, "impotence": 12.0, "capped": 12.0, "00": 12.0, "stripes": 10.3, "pensions": 10.0, "signatories": 10.0, "excuse": 10.0, "fulfill": 10.0, "attribute": 10.0, "lebanon": 10.0, "stabbed": 10.0, "expansionist": 10.0, "nationalists": 10.0, "jordan": 10.0, "underwent": 10.0, "myth": 10.0}'


可是这样又会回到之前的那个问题。也就是，两个类的特征相关度很小，oracle判定它们需要归拢，模型也很难从中学会归拢的正确逻辑。这怎么解决？


你说的方法是有道理的。我的想法是：
1. 在类的特征中使用混合策略。一部分是词频（bm25），另一部分是一个简短的summary。写summary的模型不训练，直接使用zero-shot。
2. 在生成数据时，采用我之前说的，当子类多时、相似度高时或者文章处理到一定程度时，执行归拢。
3. 把上面的“归拢理由”加到completion中。
这样是不是一种可行的、稳定的方法？
但这样我也有了一个顾虑。用zero-shot的方法生成summary可能效果不够好，那么我就需要预先训练一下这个模型。可如果我这样做，我的这个做法就很像一个trick，而不是能写在论文中的一个straight forward的方法。


抽取式确实是最可发表、最稳定的方法，但是一个类别下可能有十多个子类，考虑到prompt长度和模型的能力，最多给每个子类200token左右的额度。抽取式的方法在200次的限额下很难发挥效果吧。
我能够这样做，这样说明？
我就用两个系统，一个是现在的分类系统，一个是之前的总结系统，负责生成、更新summary。然后我的任务需要这两个模型配合、交替推理，最后达成目标。
那么最合理的方法就是两个模型联合训练。但是我现在证明了策略π指向全局最优，因此我将两个模型解耦，分类模型用策略π指导，而总结模型用强大模型指导。
这样的方法在论文中能站得住脚吗？

那也就是说，我的论文核心在于整体构建框架、分类模型、策略π引导，而bm25、混合、生成summary都只是一个结点特征的表示方式，其作为一个组件是可插拔的。
那么如果我真的要训练一个模型来生成summary，我也需要对比这三类方法，来做消融实验。
那我无论如何都需要试一下混合表示方法（bm25关键词+抽取式summary）的效果。
那我现在应该做的就是实验这种方法。
我的理解有问题吗？


那我们接下来开始实现这个混合表示方法。
1. 在updater中加一个混合模式，不要动之前的模式，新加一股模式。这个模式需要统计bm25得分，取前10个作为结点特征表示的第一部分。然后用LLM zero-shot的方式，从文章中截取“证据片段”，作为特征的第二部分。
  updater当然需要实现第一、二部分的更新。在新加一篇文章以后，更新这个“证据片段”。严格限制其长度不超过200token。
2. 增加一个混合模式的相似度计算方法。不要删掉现有的方法，而是新写一个方法，然后按照模式来自动选择对应的方法。判定归拢时机的逻辑是，相似度达标时归拢、子类够多时归拢、类别中文章数量达标时归拢。
3. 在分类模型的completion中给出归拢的原因：相似度高、子类过多放宽相似度要求等等。相对应的修改分类模型输出解析方法，不要受输出原因的影响。
这是我想到的需要修改的地方。你有什么问题吗？还有其他需要修改的地方吗？

1.1 不会出现从多篇文章提取的情况。新类只有一篇文章，后续文章分到这个类、证据更新时，也是“增量”更新。只是要注意，归拢出新类时，新类的证据片段是从多个子类的证据片段中挑选的。
1.2 tokenizer计数。
1.3 是的，让LLM提取"区分性证据"而非"概括性摘要"。
2. 这个格式可以。
3. 是的，需要在config中增加参数。
4. 最后两个，“文章数量达标”和“最后文章”虽然原因不同，但是在completion中应该原因一致，都是文章处理到了一定程度了。


1. 先说现在的问题。vLLM 不是线程安全的，但是单开一个进程是肯定没问题的，不要强行串行，这样失去了这个架构的意义。
  你开一个专门的进程进程，inference_parallel.py一样，用进程来处理vllm推理，用线程来处理topic。
2. 我仔细思考了一下，我认为我们现在走偏了。现在是要采用一个混合的结点特征表示方法，然后延迟归拢。
  首先，延迟归拢这个操作很奇怪、很trick，如果一个结点的特征表示是足够清晰的，那么其实不需要延迟归拢。我们决定归拢的条件也是人为设定，这是一个“拍脑门”的设计。
  我觉得我们应该回归之前的方法，单独用一个summary生成模型来生成结点特征，只需要稍微修改现有的总结系统的prompt，不再需要它判断是否需要生成新类，而是只负责生成、更新summary即可，其他的要求都不变。
  也就是说，我觉得我们需要回退版本。在生成数据时取消延迟归拢，直接用策略π来得到分类模型应该输出的结果。而summary的生成则用api的强大模型来完成。然后记录强大模型的输入输出，指导训练总结模型。


1. 回到之前没有延迟的版本。
2. 纯summary文本。
3. 同时进行。


1. 程序的api用 llm/deepseek_api.py，其中的run_prompts_to_texts方法实现了内部多线程并行处理、提交prompt、接收结果，直接使用即可。
2. 我们不能只用第一个path，而是需要处理多路径的情况。多路径会得到多分类结果，全部递归就行。唯一要注意的是，如果在多路径的情况下创建新类，新类对齐时不要重复；如果有多个类需要被创建，此时只用第一个path创建类和对齐。
3. 发现一个小的逻辑问题。当一个类是新类时，是不需要判断是否需要更新summary。

你不能这样处理多路径。你现在是直接循环，每次处理一个路径，而实际上，假设路径两个是A-B-C和A-B-D，那么在B结点下分类时，实际上输出的是多分类结果CD，而你这样处理就错误了。
你应该一次性传入多个路径，然后把这多个路径都给策略π，生成多分类的结果。我说的生成新类的特殊情况也是在这里，当有多个新类需要生成时，只用第一个path，既有分类又有新类时，要注意新类对应的结点。
然后对多分类的结果都要递归。你可以维护一个待处理当前结点队列。



你完全重构了生成数据的代码，但实际上，之前的代码中只是上层的步骤不对，每一步的逻辑是对的。
你现在的代码中，首先，你是从结构树中寻找一篇文章的path，而实际上文章的path已经保存在ref文件中了，不需要重新查找。
其次，你完全没有用策略π的代码，它已经被检查是没问题的了。在自上而下分类时，decide_top_down_child方法是正确的，而你现在的代码是错误的。
最后，生成summary的操作失败了，不要像你现在这样重新定义prompt，而是应该使用现有的updater系统啊，那里面有完整的prompt和多种模式，你只需要使用updater最开始的哪个模式。

我觉得你生成数据的代码有很大问题。生成数据的代码实际上和推理的流程一模一样，自上而下分类，自下而上更新总结。不一样的地方只是用策略π来得到分类结果，要加的东西就是对齐结点和记录输入输出。
你在对齐节点的时候代码有很大问题。你在大改代码之前的那个版本的逻辑是正确的。新结点对齐的时候，要对齐能覆盖结点下所有文章的、最深的那个节点。
比如一篇文章的path是A-B-C，当前root下没有结点，那么新生成的结点对齐的就应该是A-B-C，而不是A。
我之前说的让你重构代码，不是推翻全部逻辑，而是在每一步逻辑不变的情况下重写生成数据这一部分的逻辑。

诶呦不是这么简单的。我再跟你完整的说一次这个对齐的逻辑。
创建一个新的节点时，包括归拢，都需要对齐覆盖结点下所有文章的、最深的那个节点。
如果已经有了类Root - C，那么文章就分类到C。如果有了Root-A-D，那么就应该生成新类Root-A-(D,C)。
如果当前结构树为Root-D，那么生成新类Root-C，此时进行归拢，这个新归拢出来的结点就应该是B，因为B能包含CD的文章，并且B比A更深。


不对，你理解的还是不对，我给你一个完整的例子：
Root
  A
    a1
      a11 - (d1)
      a12 - (d2)
    a2 - (d3)
  B - (d4)

现在我们处理文章的顺序是3,4,1,2(随机的)
那处理流程大概就应该是
d2:
Root
  a12 - (d2)
d4:
Root
  a12 - (d2)
  B - (d4)
d1:
Root
  a1
    a11 - (d1)
    a12 - (d2)
  B - (d4)
d3:
Root
  A
    a1
      a11 - (d1)
      a12 - (d2)
    a2 - (d3)
  B - (d4)

在处理d1时，归拢a11和a12，得到a1，不是因为两个子节点在k+1层的共同祖先是a1，而是能覆盖这两个节点的结点有（A，a1），而a1是最深的。
事实上，两个子节点在k+1层的共同祖先是A，但实际上应该生成a1


当你归拢两个节点后，新节点下会出现三个节点，其中两个的对齐是相同的。我怀疑你是不是在归拢之后在新节点下继续执行创建新类了？或者是其他原因？


400行，我不知道你这个筛选是什么意思。在策略π正确的识别分类后，被你这段代码筛选掉了，然后开始错误的创建新类。
另外，生成summary的api有时候会失败。


在分类到子节点后，对齐新类时路径有问题。
比如，当且已有类别Charles Darwin - Biography，文章的正确path为Charles Darwin - Biography - Overwork, illness, and marriage。
文章先被分类到Biography，然后在创建新类后，得到了错误的对齐路径：
'Charles Darwin - Biography - Biography - Overwork, illness, and marriage'


1.当文章被分类到一个子类中，然后继续递归时，419行article_parts的前缀输入有问题，导致策略π无法正确提供分类结果。
  比如当前节点是Root-A，你应该输入，article_parts = [Root, A, B]，但你输入的是[Root - A, B]
2.生成新节点的summary时，当前节点的summary是空，这没问题，但是有时候api返回的结果是不需要更新summary。这应该是prompt的问题。
  在prompt中重新说明写总结的要求。新的summary不能与兄弟结点的summary有重叠、不能大幅超出父节点的范围、只关注和topic有关的内容、保持格式不变、简洁。


文章分类完以后，要把文章编号加到节点的citation中啊


1. 不止是创建新类的时候要把文章加进去，文章分类到现有的叶子结点时，也要文章编号加到节点的citation中。
2. 一个结点下，所有兄弟节点的summary都一模一样。



重新设计总结系统的prompt，输入的变量不变。如果父节点summary为空，把父节点summary设成topic。
现在的prompt中说明的逻辑太复杂了，我们重新设计prompt，引导模型输出推理过程。
先让模型输出与父节点summary相关的文章内容，直接从原文截取，然后长的句子用...代替。
比如xxx ... xxx 这样，表示从哪到哪的原文。这里可以不止一段，应该输出一个字典，key是编号0,1,...值是这样的原文截取。
然后再让模型从这些片段中选出哪些截取内容是与兄弟结点无关的，这里输出上面的键，也是一个列表。
再让模型根据兄弟结点无关的内容判断是否需要更新当前节点的summary（yes/no）。
最后输出更新后的summary。
这样引导模型走完整个思考流程。
而在生成数据时，我们要让api补全我们想要的答案。我们在prompt的最后额外告诉api当前节点的path是什么，让api返回的结果向这个方向靠拢。


1. 不要直接截断文章长度，而是把文章的长度作为一个参数加入到config中，然后在生成数据和推理时，根据token数来截断文章。
2. 你现在只记录了分类系统的输入和输出，生成了分类系统的数据，但实际上我们应该记录分类系统和总结系统的输入输出，然后分别训练这两个模型。
3. 我们只在生成总结系统的数据时，给api一个提示，在推理时是没有提示的。因此，这个提示应该单独的加在最后面，并且在记录输入输出时不应该有这个提升。
  也就是说，我们要先记录输入，然后单独加一个结果提示，然后给api生成结果，然后记录输出。



1. 253行，api的回答被截断了，要么是我们的程序截断的，要么是限制了api输出的token长度。增大这个长度限制。
2. 你现在的文章处理逻辑有一些小瑕疵。不是有了分类结果就不创建新类了，而是要看分类结果的个数是否等于path个数。有可能出现分类到现有类、并且需要创建新类的情况。
  此时，创建新类就要选择没有包含在分类结果中的第一个path来创建新类。
  比如文章path有：A-B-C,A-B-D,E,F。当前结构树为Root-A,那么策略π得到分类结果为0，那么前两个path已经包含在分类结果中了，后两个没有，因此选第一个后两个中的第一个创建新类Root-E。
  相对应的，创建出新类E后也有可能执行归拢操作。分到现有类的要正常递归分类。分类系统的正确输出也要正确的记录下来。
  提醒一下，比如文章path有：A-B-C,A-B-D,E,F。当前结构树为Root-C，那么此时策略π得到分类结果为0，这时只有第一个path包含在分类结果中，后三个都不在，然后需要创建新类Root-D，再归拢出结点A。



1. 你还是没有把文章加到叶子结点中。比如当前结构树为：
Root
  A
    a1
    a2
一篇文章的path为 A-a1，处理完这篇文章后，成功记录了将文章分到A、a1的两条分类记录，但是文章没有在a1的citation中。

2. 你现在的分类过程还保留着“使用第一条路径构造article_parts”这种操作。实际上，你直接把所有的path都给策略π即可。
  selected_indices的逻辑也不明所以。按上述例子来说，一篇文章四个path，前两个包含在分类结果A中，后两个不在，但first_uncovered_path仍为空


在更新summary这一步，你没有自下而上传递。
比如一篇文章被依次分类到A-B-C，那么需要把文章作为新内容，更新结点C的summary，然后继续传递，把C作为新内容，更新B的summary。直到一个结点，updater判断不需要更新或者到根节点为止。不需要更新根节点的summary。



你没有正确的处理 NEEDS_UPDATE：No时的情况。这并不是非法输出，而是正确的、你应该记录的输出，此时的summary不变就好。



1. 在updater的prompt中加入这些说明：在输出的第一部分，截取文章片段时，只输出开头几个单词和结尾的几个单词，中间的部分都用...代替。截取时尽量合并成长的片段，不要太过零散。
2. 在生成数据时，写成两个模式。一个模式就是现在的代码，用api获取总结模型的输出，同时记录总结模型数据和分类模型数据。另一个模型是用两张显卡加载训练好的总结模型，直接获取总结模型的输出，只记录分类模型数据。这两个模式的整体逻辑一样，只是获取总结的方式有区别。
3. 在训练两个模型前，平衡两个数据集的样本分布。
  在训练总结模型时，区分需要更新的样本和不需要更新的样本，如果不需要更新的样本数量不足，进行上采样，以达到目标比例。
  在训练分类模型时，区分需要创建新类、需要归拢、只分类的训练样本，如果前面两个的样本数量不足，进行上采样，以达到目标比例。


为什么还是没有改过来。生成数据的时候，如果这个类是新创建的，那么就不需要判断这个节点是否需要更新，直接从这个节点的父节点开始自下而上更新即可。


在训练分类模型时，loss下降慢，eval loss明显高于train loss。用训练完的模型推理时，模型没有按照要求的格式输出，因此我们做如下修改：
1. 取消分类模型中的动态格式说明。现在的数据集中没有写MERGE_REASON，因此直接把格式写死即可。
2. 加大模型输出的长度。现在的模型输出太短，权重低，非常不安全。我们还是要模型输出三行的判断，但每一行的token要变多。
3. 暂且不要在prompt加入结构信息了，然后重新规划prompt内容，让逻辑更简洁、清晰一些。



总结模型可以直接使用8B
生成模型需要训练，在completion中加入理由说明，理由用8B模型生成
带电话回来，测试prompt



我需要对代码做一个比较大的改动。
1. 修改分类模型的prompt，让模型在输出结果json前，先输出一段推理内容，再输出json结果。
2. 同步修改分类模型的输出解析。
3. 阅读data/prepare_dataset_oracle.py，做如下修改：
  1）总结模型直接用基础模型，即qwen3-8B。后续的推理代码也用基础模型作为总结模型。
  2）按照现在的这个生成数据的逻辑，用策略π获取结果，这不用变。你需要再重新设计一个prompt，用基础模型，输入分类模型的输入和用策略π得到的结果，让模型根据结果补充推理，然后按照分类模型的输出格式，构建输出。
对此你有什么问题吗？


1. 输出格式就按你说的这样即可，可以加入分隔符，便于解析。
2. 训练时需要将推理内容也加入监督，也就是说，我想要训练后的分类模型能够输出推理内容，让最终结果更可靠。
3. 这个理解是对的，直接用qwen3-8B做总结。
4. 这个不用大改代码，你只需要修改配置参数，指定基础模型就行了。
5. 这样做没问题。
最后补充一点，刚才忘记讲了，由于分类模型输出格式的改变，平衡数据比例的代码也需要相应修改。
